<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, user-scalable=no">
<title>ResumeAI Pro</title>
<meta name="description" content="Premium AI-powered resume maker with Apple HIG design">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@500;600;700;800&display=swap" rel="stylesheet">
<script type="importmap">
{
  "imports": {
    "firebase/auth": "https://www.gstatic.com/firebasejs/11.0.1/firebase-auth.js",
    "firebase/app": "https://www.gstatic.com/firebasejs/11.0.1/firebase-app.js"
  }
}
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/mammoth/1.6.0/mammoth.browser.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
<script src="https://unpkg.com/lucide@latest"></script>
<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<script src="env.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#0F1624;--bg2:#131B2E;--surface:#1A2440;--surface2:#222E4A;--surface3:#2A3650;
  --primary:#6366F1;--primary-hover:#5558E6;--primary-dim:rgba(99,102,241,0.15);
  --amber:#F59E0B;--amber-dim:rgba(245,158,11,0.15);--green:#10B981;--red:#EF4444;
  --white:#FFFFFF;--text:#E8EDF5;--text2:#9CA3BB;--text3:#6B7280;--border:rgba(255,255,255,0.08);
  --radius-card:16px;--radius-btn:12px;--radius-inp:10px;--radius-chip:8px;
  --font-display:'Plus Jakarta Sans',-apple-system,BlinkMacSystemFont,sans-serif;
  --font-body:'Inter',-apple-system,BlinkMacSystemFont,sans-serif;
  --ease-spring:cubic-bezier(0.34,1.56,0.64,1);--ease-out:cubic-bezier(0.4,0,0.2,1);
  --shadow:0 8px 32px rgba(0,0,0,0.3);--glass:rgba(26,36,64,0.85);
}
.light{--bg:#FFFFFF;--bg2:#F9FAFB;--surface:#F3F4F6;--surface2:#E5E7EB;--surface3:#D1D5DB;
  --text:#111827;--text2:#4B5563;--text3:#9CA3AF;--border:rgba(0,0,0,0.08);
  --glass:rgba(255,255,255,0.85);--shadow:0 8px 32px rgba(0,0,0,0.1);
}
html{font-size:16px;scroll-behavior:smooth}
body{font-family:var(--font-body);background:var(--bg);color:var(--text);min-height:100vh;overflow-x:hidden;line-height:1.5;-webkit-font-smoothing:antialiased}
h1,h2,h3,h4,h5,h6{font-family:var(--font-display);font-weight:700;letter-spacing:-0.02em}
a{color:var(--primary);text-decoration:none}
button{cursor:pointer;font-family:var(--font-body);border:none;background:none;color:inherit;-webkit-tap-highlight-color:transparent}
input,textarea,select{font-family:var(--font-body);font-size:1rem;color:var(--text);background:var(--surface);border:1.5px solid var(--border);border-radius:var(--radius-inp);padding:14px 16px;outline:none;width:100%;transition:border-color .2s var(--ease-out)}
input:focus,textarea:focus{border-color:var(--primary);box-shadow:0 0 0 3px var(--primary-dim)}
input::placeholder,textarea::placeholder{color:var(--text3)}
label{font-size:0.8125rem;font-weight:600;color:var(--text2);margin-bottom:6px;display:block;letter-spacing:0.01em}
img{max-width:100%;display:block}
::-webkit-scrollbar{width:6px}::-webkit-scrollbar-track{background:transparent}::-webkit-scrollbar-thumb{background:var(--surface3);border-radius:3px}
.glass{background:var(--glass);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border-bottom:1px solid var(--border)}
.glass-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);padding:24px;backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px)}
.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);padding:24px}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;padding:12px 24px;border-radius:var(--radius-btn);font-weight:600;font-size:0.9375rem;transition:all .2s var(--ease-out);min-height:44px;min-width:44px;position:relative;overflow:hidden;letter-spacing:-0.01em}
.btn:active{transform:scale(0.97)}
.btn-primary{background:var(--primary);color:#fff}.btn-primary:hover{background:var(--primary-hover)}
.btn-ghost{background:transparent;color:var(--text2)}.btn-ghost:hover{background:var(--surface2);color:var(--text)}
.btn-outline{border:1.5px solid var(--border);color:var(--text);background:transparent}.btn-outline:hover{background:var(--surface2);border-color:var(--text3)}
.btn-amber{background:var(--amber);color:#000}.btn-amber:hover{filter:brightness(1.1)}
.btn-full{width:100%}
.btn-sm{padding:8px 16px;font-size:0.8125rem;min-height:36px}
.btn-lg{padding:16px 32px;font-size:1.0625rem}
.btn-icon{width:44px;height:44px;padding:0;display:flex;align-items:center;justify-content:center;border-radius:var(--radius-btn)}
.badge{display:inline-flex;align-items:center;gap:4px;padding:4px 12px;border-radius:100px;font-size:0.75rem;font-weight:600}
.badge-premium{background:var(--amber-dim);color:var(--amber)}
.badge-free{background:var(--primary-dim);color:var(--primary)}
.badge-green{background:rgba(16,185,129,0.15);color:var(--green)}
.badge-red{background:rgba(239,68,68,0.15);color:var(--red)}
.chip{display:inline-flex;align-items:center;gap:6px;padding:6px 14px;border-radius:var(--radius-chip);background:var(--surface2);font-size:0.8125rem;cursor:pointer;transition:all .15s var(--ease-out);border:1px solid transparent}
.chip:hover{background:var(--surface3)}
.chip-active{border-color:var(--primary);background:var(--primary-dim);color:var(--primary)}
.chip-green{background:rgba(16,185,129,0.15);color:var(--green);border-color:rgba(16,185,129,0.3)}
.chip-red{background:rgba(239,68,68,0.15);color:var(--red);border-color:rgba(239,68,68,0.3)}
.flex{display:flex}.flex-col{flex-direction:column}.items-center{align-items:center}.justify-between{justify-content:space-between}.justify-center{justify-content:center}.gap-4{gap:4px}.gap-8{gap:8px}.gap-12{gap:12px}.gap-16{gap:16px}.gap-24{gap:24px}.gap-32{gap:32px}.flex-1{flex:1}.flex-wrap{flex-wrap:wrap}
.text-center{text-align:center}.text-right{text-align:right}.text-2{color:var(--text2)}.text-3{color:var(--text3)}.text-sm{font-size:0.8125rem}.text-xs{font-size:0.75rem}.text-lg{font-size:1.125rem}.text-xl{font-size:1.25rem}.text-2xl{font-size:1.5rem}.text-3xl{font-size:2rem}.font-bold{font-weight:700}.font-semibold{font-weight:600}
.mt-8{margin-top:8px}.mt-16{margin-top:16px}.mt-24{margin-top:24px}.mt-32{margin-top:32px}.mb-8{margin-bottom:8px}.mb-16{margin-bottom:16px}.mb-24{margin-bottom:24px}.ml-8{margin-left:8px}.mr-8{margin-right:8px}
.p-16{padding:16px}.p-24{padding:24px}.py-8{padding-top:8px;padding-bottom:8px}.py-16{padding-top:16px;padding-bottom:16px}.px-16{padding-left:16px;padding-right:16px}
.w-full{width:100%}.relative{position:relative}.hidden{display:none!important}
.grid{display:grid}.gap-16{gap:16px}
.animate-in{animation:fadeIn .4s var(--ease-out)}
@keyframes fadeIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}
@keyframes slideUp{from{transform:translateY(100%)}to{transform:translateY(0)}}
@keyframes slideDown{from{transform:translateY(-100%)}to{transform:translateY(0)}}
@keyframes shimmer{0%{background-position:-200px 0}100%{background-position:200px 0}}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:0.5}}
@keyframes spin{to{transform:rotate(360deg)}}
@keyframes ringFill{from{stroke-dashoffset:565.48}to{stroke-dashoffset:0}}
.skeleton{background:linear-gradient(90deg,var(--surface2) 25%,var(--surface3) 50%,var(--surface2) 75%);background-size:200px 100%;animation:shimmer 1.5s infinite;border-radius:var(--radius-inp);height:20px;width:100%}
.skeleton-card{height:200px;width:100%;border-radius:var(--radius-card)}
.spinner{width:20px;height:20px;border:2px solid var(--border);border-top-color:var(--primary);border-radius:50%;animation:spin .6s linear infinite}
.toast{position:fixed;top:24px;left:50%;transform:translateX(-50%) translateY(-100px);background:var(--surface);border:1px solid var(--border);backdrop-filter:blur(20px);padding:14px 24px;border-radius:100px;font-size:0.875rem;font-weight:500;z-index:10000;transition:transform .4s var(--ease-spring);box-shadow:var(--shadow);display:flex;align-items:center;gap:10px;white-space:nowrap}
.toast.show{transform:translateX(-50%) translateY(0)}@media(max-width:640px){.toast{top:auto;bottom:24px;left:16px;right:16px;transform:translateY(100px);border-radius:var(--radius-btn)}.toast.show{transform:translateY(0)}}

/* Layout */
#app{max-width:100vw;overflow:hidden}
.navbar{position:fixed;top:0;left:0;right:0;z-index:1000;height:64px;display:flex;align-items:center;justify-content:space-between;padding:0 24px}
.navbar-brand{display:flex;align-items:center;gap:10px;font-family:var(--font-display);font-weight:800;font-size:1.125rem;color:var(--white)}
.navbar-brand .brand-icon{width:32px;height:32px;background:var(--primary);border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:1rem}
.navbar-actions{display:flex;align-items:center;gap:12px}
.navbar-actions .user-avatar{width:36px;height:36px;border-radius:50%;object-fit:cover;border:2px solid var(--border);cursor:pointer}
.navbar-actions .user-avatar-placeholder{width:36px;height:36px;border-radius:50%;background:var(--primary-dim);color:var(--primary);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:0.875rem;cursor:pointer}

/* Landing */
.landing{min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:24px;text-align:center}
.landing-icon{width:80px;height:80px;background:var(--primary);border-radius:24px;display:flex;align-items:center;justify-content:center;margin-bottom:32px;box-shadow:0 0 40px rgba(99,102,241,0.3)}
.landing h1{font-size:clamp(2rem,5vw,3rem);margin-bottom:16px;line-height:1.2}
.landing p{font-size:1.125rem;color:var(--text2);max-width:480px;margin-bottom:40px;line-height:1.6}
.landing .features{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;max-width:680px;width:100%;margin-bottom:40px}
.landing .feature-item{padding:16px;border-radius:var(--radius-card);background:var(--surface);border:1px solid var(--border);text-align:left}
.landing .feature-item .fi-icon{width:40px;height:40px;border-radius:12px;background:var(--primary-dim);color:var(--primary);display:flex;align-items:center;justify-content:center;margin-bottom:12px}
.landing .feature-item h3{font-size:0.9375rem;margin-bottom:4px}
.landing .feature-item p{font-size:0.8125rem;margin-bottom:0;color:var(--text3)}

/* Bottom Tabs - Mobile */
.bottom-tabs{display:none;position:fixed;bottom:0;left:0;right:0;z-index:1000;height:64px;background:var(--glass);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border-top:1px solid var(--border);padding:0 8px}
.bottom-tabs .tab-item{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;padding:8px 0;color:var(--text3);font-size:0.625rem;font-weight:500;transition:color .2s;border-radius:var(--radius-btn);cursor:pointer;-webkit-tap-highlight-color:transparent;min-height:56px;position:relative}
.bottom-tabs .tab-item svg{width:22px;height:22px;transition:all .2s}
.bottom-tabs .tab-item.active{color:var(--primary)}.bottom-tabs .tab-item.active svg{color:var(--primary)}
.bottom-tabs .tab-item.active::after{content:'';position:absolute;top:0;width:20px;height:3px;background:var(--primary);border-radius:0 0 3px 3px}
@media(max-width:768px){.bottom-tabs{display:flex}.navbar{padding:0 16px}.navbar .nav-links{display:none}.main-content{padding-bottom:72px}}

/* Main content */
.main-content{padding-top:80px;max-width:1200px;margin:0 auto;padding-left:24px;padding-right:24px}
.page{display:none;animation:fadeIn .3s var(--ease-out)}.page.active{display:block}

/* Builder */
.builder-layout{display:flex;gap:32px;align-items:flex-start}
.builder-form{flex:1;max-width:640px}
.builder-preview{flex:1;position:sticky;top:80px;max-height:calc(100vh - 100px);overflow-y:auto}@media(max-width:1024px){.builder-preview{display:none}.builder-form{max-width:100%}}

.step-indicator{display:flex;align-items:center;gap:0;margin-bottom:32px;overflow-x:auto;padding:4px 0}
.step-dot{display:flex;align-items:center;gap:8px;cursor:pointer;white-space:nowrap;padding:8px 4px;min-width:44px;min-height:44px;justify-content:center;position:relative}
.step-dot .dot{width:12px;height:12px;border-radius:50%;background:var(--surface3);transition:all .3s var(--ease-spring);flex-shrink:0}
.step-dot .dot.active{background:var(--primary);box-shadow:0 0 12px rgba(99,102,241,0.4);width:14px;height:14px}
.step-dot .dot.done{background:var(--green);width:14px;height:14px}
.step-dot .step-label{font-size:0.75rem;color:var(--text3);font-weight:500}
.step-dot .dot.active+.step-label,.step-dot .dot.done+.step-label{color:var(--text)}
.step-connector{flex:1;height:2px;background:var(--surface3);min-width:16px;margin:0 4px}.step-connector.done{background:var(--green)}

.step-card{display:none;animation:fadeIn .35s var(--ease-spring)}.step-card.active{display:block}
.step-card .step-title{font-size:1.5rem;margin-bottom:4px}
.step-card .step-subtitle{color:var(--text2);font-size:0.9375rem;margin-bottom:24px}
.step-card .form-group{margin-bottom:20px}
.step-card .form-group .floating-label{position:relative}
.step-card .form-group .clear-btn{position:absolute;right:12px;top:50%;transform:translateY(-50%);width:20px;height:20px;border-radius:50%;background:var(--surface3);display:flex;align-items:center;justify-content:center;font-size:0.75rem;cursor:pointer;opacity:0;transition:opacity .15s}
.step-card .form-group .clear-btn.show{opacity:1}

.entry-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);padding:20px;margin-bottom:16px;position:relative}
.entry-card .entry-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px}
.entry-card .entry-header h4{font-size:0.9375rem}
.entry-card .entry-remove{width:36px;height:36px;display:flex;align-items:center;justify-content:center;border-radius:var(--radius-btn);color:var(--red);cursor:pointer}
.entry-card .entry-remove:hover{background:rgba(239,68,68,0.1)}
.entry-card .form-group{margin-bottom:12px}
.entry-card .form-group:last-child{margin-bottom:0}
.entry-card-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}@media(max-width:480px){.entry-card-grid{grid-template-columns:1fr}}

.skills-tags{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
.skills-tags .skill-tag{padding:8px 16px;border-radius:var(--radius-chip);background:var(--surface2);font-size:0.875rem;cursor:pointer;transition:all .15s;display:flex;align-items:center;gap:6px;border:1px solid transparent}
.skills-tags .skill-tag.selected{border-color:var(--primary);background:var(--primary-dim);color:var(--primary)}
.skills-tags .skill-tag .remove{width:18px;height:18px;border-radius:50%;background:var(--surface3);display:flex;align-items:center;justify-content:center;font-size:0.625rem;cursor:pointer}

/* Preview Panel */
.preview-panel{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);overflow:hidden}
.preview-header{display:flex;justify-content:space-between;align-items:center;padding:16px 20px;border-bottom:1px solid var(--border)}
.preview-header h3{font-size:0.9375rem;font-weight:600}
.preview-template-select{display:flex;gap:8px;flex-wrap:wrap}
.preview-template-select .tmpl-btn{padding:6px 12px;border-radius:var(--radius-chip);font-size:0.75rem;font-weight:500;cursor:pointer;transition:all .15s;border:1px solid var(--border);background:transparent;color:var(--text2)}
.preview-template-select .tmpl-btn.active{border-color:var(--primary);background:var(--primary-dim);color:var(--primary)}
.preview-body{padding:32px;min-height:400px;position:relative}
.preview-watermark{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%) rotate(-30deg);font-size:1.5rem;font-weight:800;color:var(--text3);opacity:0.12;pointer-events:none;white-space:nowrap;z-index:1}

/* Resume Templates */
.resume-a4{background:#fff;color:#1a1a2e;padding:40px 36px;max-width:595px;margin:0 auto;box-shadow:0 2px 16px rgba(0,0,0,0.08);position:relative;font-family:var(--font-body)}
.resume-a4 *{color:#1a1a2e}
.resume-classic .r-name{font-size:1.5rem;font-weight:700;margin-bottom:4px;font-family:serif}
.resume-classic .r-title{font-size:0.9375rem;color:#555;margin-bottom:16px;font-family:serif}
.resume-classic .r-section-title{font-size:0.8125rem;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;border-bottom:1px solid #ddd;padding-bottom:6px;margin:20px 0 10px;font-family:serif}
.resume-classic .r-item-title{font-weight:600;font-size:0.875rem}
.resume-classic .r-item-sub{font-size:0.8125rem;color:#555}
.resume-classic .r-item-desc{font-size:0.8125rem;color:#444;margin-top:4px;line-height:1.5}

.resume-modern .r-sidebar{width:32%;float:left;padding-right:20px;background:#1a1a2e;margin:-40px 20px -40px -36px;padding:40px 20px 40px 36px;color:#fff;min-height:400px}
.resume-modern .r-sidebar *{color:#fff}
.resume-modern .r-main{margin-left:38%}
.resume-modern .r-name{font-size:1.5rem;font-weight:700;margin-bottom:4px}
.resume-modern .r-title{font-size:0.9375rem;opacity:0.8;margin-bottom:20px}
.resume-modern .r-section-title{font-size:0.75rem;font-weight:700;text-transform:uppercase;letter-spacing:0.1em;border-bottom:1px solid rgba(255,255,255,0.2);padding-bottom:6px;margin:20px 0 10px}
.resume-modern .r-sidebar .r-section-title{border-color:rgba(255,255,255,0.15)}
.resume-modern .r-item-title{font-weight:600;font-size:0.875rem}
.resume-modern .r-item-sub{font-size:0.8125rem;color:var(--text2)}
.resume-modern .r-item-desc{font-size:0.8125rem;color:#444;margin-top:4px;line-height:1.5}
.resume-modern .r-sidebar .r-item-desc,.resume-modern .r-sidebar .r-item-sub{color:rgba(255,255,255,0.7)}
.resume-modern::after{content:'';display:table;clear:both}

.resume-executive{position:relative}
.resume-executive .r-header{background:#1a1a2e;margin:-40px -36px 24px;padding:40px 36px 32px;color:#fff}
.resume-executive .r-header *{color:#fff}
.resume-executive .r-name{font-size:2rem;font-weight:800;margin-bottom:2px}
.resume-executive .r-title{font-size:1rem;opacity:0.8;margin-bottom:8px}
.resume-executive .r-contact{display:flex;flex-wrap:wrap;gap:12px;font-size:0.8125rem;opacity:0.7;margin-top:8px}
.resume-executive .r-section-title{font-size:0.8125rem;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;border-bottom:2px solid var(--primary);padding-bottom:6px;margin:20px 0 10px;color:#1a1a2e}
.resume-executive .r-item-title{font-weight:600;font-size:0.9375rem}
.resume-executive .r-item-sub{font-size:0.8125rem;color:#555}
.resume-executive .r-item-desc{font-size:0.8125rem;color:#444;margin-top:4px}

.resume-minimal{padding:48px 40px}
.resume-minimal .r-name{font-size:1.75rem;font-weight:300;letter-spacing:0.02em;margin-bottom:2px}
.resume-minimal .r-title{font-size:0.9375rem;color:#888;margin-bottom:24px;font-weight:400}
.resume-minimal .r-divider{height:1px;background:#e0e0e0;margin:16px 0}
.resume-minimal .r-section-title{font-size:0.6875rem;font-weight:600;text-transform:uppercase;letter-spacing:0.15em;color:#999;margin-bottom:8px}
.resume-minimal .r-item-title{font-weight:500;font-size:0.875rem}
.resume-minimal .r-item-sub{font-size:0.8125rem;color:#777}
.resume-minimal .r-item-desc{font-size:0.8125rem;color:#555;margin-top:4px}

.resume-tech .r-grid{display:grid;grid-template-columns:100px 1fr;gap:4px 16px}
.resume-tech .r-section-label{font-size:0.75rem;font-weight:700;text-transform:uppercase;letter-spacing:0.1em;color:var(--primary);font-family:'SF Mono','Fira Code',monospace;text-align:right;padding-top:4px}
.resume-tech .r-section-content{border-left:2px solid #eee;padding-left:16px;padding-bottom:16px}
.resume-tech .r-name{font-size:1.625rem;font-weight:700;margin-bottom:2px}
.resume-tech .r-title{font-size:0.9375rem;color:#666;margin-bottom:20px}
.resume-tech .r-item-title{font-weight:600;font-size:0.875rem}
.resume-tech .r-item-sub{font-size:0.8125rem;color:#666}
.resume-tech .r-item-desc{font-size:0.8125rem;color:#444;margin-top:4px}

/* Score page */
.score-zone{border:2px dashed var(--border);border-radius:var(--radius-card);padding:48px 24px;text-align:center;cursor:pointer;transition:all .2s;background:var(--surface);position:relative}
.score-zone:hover,.score-zone.dragover{border-color:var(--primary);background:var(--primary-dim)}
.score-zone .upload-icon{width:64px;height:64px;border-radius:50%;background:var(--primary-dim);color:var(--primary);display:flex;align-items:center;justify-content:center;margin:0 auto 16px}
.score-zone input[type=file]{position:absolute;inset:0;opacity:0;cursor:pointer}

.score-ring{width:200px;height:200px;position:relative;margin:0 auto}
.score-ring svg{transform:rotate(-90deg)}
.score-ring .score-text{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center}
.score-ring .score-text .score-num{font-size:3rem;font-weight:800;font-family:var(--font-display);line-height:1}
.score-ring .score-text .score-label{font-size:0.8125rem;color:var(--text2)}

.section-bar{display:flex;align-items:center;gap:12px;margin-bottom:10px}
.section-bar .bar-label{font-size:0.8125rem;font-weight:500;width:100px;flex-shrink:0}
.section-bar .bar-track{flex:1;height:8px;background:var(--surface2);border-radius:4px;overflow:hidden}
.section-bar .bar-fill{height:100%;border-radius:4px;transition:width .6s var(--ease-out)}
.section-bar .bar-score{font-size:0.8125rem;font-weight:600;width:32px;text-align:right}

.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.6);z-index:5000;display:none;align-items:flex-end;justify-content:center;backdrop-filter:blur(4px)}
.modal-overlay.open{display:flex}
@media(min-width:768px){.modal-overlay{align-items:center}}
.modal-sheet{background:var(--surface);width:100%;max-width:560px;max-height:85vh;border-radius:var(--radius-card) var(--radius-card) 0 0;overflow-y:auto;animation:slideUp .4s var(--ease-spring);position:relative}
.modal-sheet .grabber{width:40px;height:4px;background:var(--surface3);border-radius:2px;margin:12px auto 8px;flex-shrink:0}
@media(min-width:768px){.modal-sheet{border-radius:var(--radius-card);max-height:80vh}.modal-sheet .grabber{display:none}}
.modal-sheet .modal-header{display:flex;justify-content:space-between;align-items:center;padding:20px 24px 0}
.modal-sheet .modal-header h2{font-size:1.25rem}
.modal-sheet .modal-body{padding:20px 24px 32px}
.modal-close{width:44px;height:44px;display:flex;align-items:center;justify-content:center;border-radius:50%;cursor:pointer;color:var(--text2)}
.modal-close:hover{background:var(--surface2)}

/* Dashboard */
.dashboard-grid{display:grid;gap:16px}
.dashboard-item{display:flex;align-items:center;gap:16px;padding:16px 20px;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);cursor:pointer;transition:all .15s;position:relative}
.dashboard-item:hover{background:var(--surface2)}
.dashboard-item .di-icon{width:44px;height:44px;border-radius:12px;background:var(--primary-dim);color:var(--primary);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.dashboard-item .di-info{flex:1;min-width:0}
.dashboard-item .di-info h4{font-size:0.9375rem;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.dashboard-item .di-info p{font-size:0.8125rem;color:var(--text2)}
.dashboard-item .di-actions{display:flex;gap:8px;flex-shrink:0}
.dashboard-empty{text-align:center;padding:64px 24px}
.dashboard-empty .de-icon{width:80px;height:80px;border-radius:50%;background:var(--surface2);display:flex;align-items:center;justify-content:center;margin:0 auto 24px;color:var(--text3)}

/* Action Sheet */
.action-sheet{position:fixed;bottom:0;left:0;right:0;z-index:6000;background:var(--surface);border-radius:var(--radius-card) var(--radius-card) 0 0;animation:slideUp .4s var(--ease-spring);padding:8px 0 24px;max-width:400px;margin:0 auto}
.action-sheet .as-grabber{width:40px;height:4px;background:var(--surface3);border-radius:2px;margin:12px auto 8px}
.action-sheet .as-item{padding:14px 24px;font-size:0.9375rem;cursor:pointer;transition:background .15s;display:flex;align-items:center;gap:12px;min-height:44px}
.action-sheet .as-item:hover{background:var(--surface2)}
.action-sheet .as-item.danger{color:var(--red)}
.action-sheet .as-item .as-icon{width:24px;height:24px;display:flex;align-items:center;justify-content:center;flex-shrink:0;color:var(--text2)}

@media(prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:0.01ms!important;animation-iteration-count:1!important;transition-duration:0.01ms!important}}

/* Coach marks */
.coach-overlay{position:fixed;inset:0;z-index:9000;background:rgba(0,0,0,0.7);display:none}
.coach-overlay.open{display:block}
.coach-card{position:absolute;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-card);padding:24px;max-width:320px;box-shadow:var(--shadow)}
.coach-card .coach-title{font-size:1rem;font-weight:700;margin-bottom:4px}
.coach-card .coach-desc{font-size:0.875rem;color:var(--text2);margin-bottom:16px}
.coach-card .coach-actions{display:flex;justify-content:space-between;align-items:center}

.ai-learned{display:inline-flex;align-items:center;gap:6px;padding:4px 14px;border-radius:100px;background:rgba(99,102,241,0.15);color:var(--primary);font-size:0.75rem;font-weight:600;animation:fadeIn .3s var(--ease-out)}
details summary{cursor:pointer;user-select:none}details summary svg[data-lucide="chevron-down"]{transition:transform .25s var(--ease-out)}details[open] summary svg[data-lucide="chevron-down"]{transform:rotate(180deg)}
</style>
</head>
<body>
<div id="app">
  <!-- Toast -->
  <div class="toast" id="toast"></div>

  <!-- Navbar -->
  <nav class="navbar glass" id="navbar">
    <div class="navbar-brand" onclick="showPage('home')">
      <div class="brand-icon" data-lucide="sparkles"></div>
      ResumeAI Pro
    </div>
    <div class="navbar-actions" id="navActions">
      <div class="nav-links" style="display:flex;gap:8px;margin-right:16px">
        <button class="btn btn-ghost btn-sm" onclick="showPage('home')">Home</button>
        <button class="btn btn-ghost btn-sm" onclick="showPage('builder')">Builder</button>
        <button class="btn btn-ghost btn-sm" onclick="showPage('score')">Score</button>
        <button class="btn btn-ghost btn-sm" onclick="showPage('dashboard')">Dashboard</button>
      </div>
      <button class="btn btn-ghost btn-icon" onclick="toggleTheme()" id="themeToggle" data-lucide="moon"></button>
      <div id="authArea">
        <button class="btn btn-primary btn-sm" onclick="signIn()" id="signInBtn">Sign In</button>
      </div>
    </div>
  </nav>

  <!-- Bottom Tabs -->
  <div class="bottom-tabs" id="bottomTabs">
    <div class="tab-item active" data-tab="home" onclick="showPage('home')">
      <span data-lucide="home"></span><span>Home</span>
    </div>
    <div class="tab-item" data-tab="builder" onclick="showPage('builder')">
      <span data-lucide="file-text"></span><span>Builder</span>
    </div>
    <div class="tab-item" data-tab="score" onclick="showPage('score')">
      <span data-lucide="target"></span><span>Score</span>
    </div>
    <div class="tab-item" data-tab="dashboard" onclick="showPage('dashboard')">
      <span data-lucide="layout-dashboard"></span><span>Dashboard</span>
    </div>
  </div>

  <div class="main-content" id="mainContent">

    <!-- Landing / Home -->
    <div class="page active" id="page-home">
      <div class="landing" id="landingView">
        <div class="landing-icon" data-lucide="sparkles" style="width:40px;height:40px;stroke-width:1.5;color:#fff"></div>
        <h1>Build Your Perfect<br>Resume with AI</h1>
        <p>ATS-optimized, beautifully designed resumes powered by artificial intelligence. Get hired faster.</p>
        <div class="features">
          <div class="feature-item"><div class="fi-icon" data-lucide="wand-2" style="width:20px;height:20px"></div><h3>AI-Powered</h3><p>Smart suggestions, bullet rewrites, and ATS scoring</p></div>
          <div class="feature-item"><div class="fi-icon" data-lucide="layout-grid" style="width:20px;height:20px"></div><h3>5 Templates</h3><p>Apple-grade designs for every industry</p></div>
          <div class="feature-item"><div class="fi-icon" data-lucide="scroll-text" style="width:20px;height:20px"></div><h3>ATS Score</h3><p>Get scored and optimized for any job</p></div>
          <div class="feature-item"><div class="fi-icon" data-lucide="download" style="width:20px;height:20px"></div><h3>PDF Export</h3><p>Clean A4 PDF with one click</p></div>
        </div>
        <button class="btn btn-primary btn-lg" onclick="continueWithGoogle()" id="landingCta">
          <span data-lucide="chrome" style="width:20px;height:20px"></span> Continue with Google
        </button>
        <p style="margin-top:16px;font-size:0.8125rem;color:var(--text3)">Free to use. No credit card required.</p>
      </div>
    </div>

    <!-- Builder -->
    <div class="page" id="page-builder">
      <div class="builder-layout">
        <div class="builder-form">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px">
            <h2>Build Your Resume</h2>
            <div style="display:flex;gap:8px;align-items:center">
              <span id="aiLearnedBadge" class="ai-learned" style="display:none"><span data-lucide="sparkles" style="width:14px;height:14px"></span> AI learned your style</span>
              <button class="btn btn-ghost btn-sm" onclick="togglePreview()" id="previewToggle">
                <span data-lucide="eye" style="width:16px;height:16px"></span> Preview
              </button>
            </div>
          </div>

          <!-- Step Indicator -->
          <div class="step-indicator" id="stepIndicator"></div>

          <!-- Step 1 - Personal Info -->
          <div class="step-card active" data-step="1">
            <h3 class="step-title">Personal Info</h3>
            <p class="step-subtitle">Let's start with the basics</p>
            <div class="form-group">
              <label>Full Name</label>
              <input type="text" id="fName" placeholder="e.g. John Doe" oninput="autoSave();updatePreview()">
            </div>
            <div class="form-group">
              <label>Job Title</label>
              <input type="text" id="fTitle" placeholder="e.g. Software Engineer" oninput="autoSave();updatePreview()">
            </div>
            <div class="entry-card-grid">
              <div class="form-group">
                <label>Email</label>
                <input type="email" id="fEmail" placeholder="john@example.com" oninput="autoSave();updatePreview()">
              </div>
              <div class="form-group">
                <label>Phone</label>
                <input type="tel" id="fPhone" placeholder="+1 234 567 890" oninput="autoSave();updatePreview()">
              </div>
            </div>
            <div class="form-group">
              <label>Location</label>
              <input type="text" id="fLocation" placeholder="e.g. San Francisco, CA" oninput="autoSave();updatePreview()">
            </div>
            <div class="entry-card-grid">
              <div class="form-group">
                <label>LinkedIn</label>
                <input type="url" id="fLinkedin" placeholder="linkedin.com/in/..." oninput="autoSave()">
              </div>
              <div class="form-group">
                <label>Portfolio</label>
                <input type="url" id="fPortfolio" placeholder="github.com/..." oninput="autoSave()">
              </div>
            </div>
            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <span></span>
              <button class="btn btn-primary" onclick="nextStep(2)">Next <span data-lucide="arrow-right" style="width:18px;height:18px"></span></button>
            </div>
          </div>

          <!-- Step 2 - Summary -->
          <div class="step-card" data-step="2">
            <h3 class="step-title">Professional Summary</h3>
            <p class="step-subtitle">A compelling overview of your career</p>
            <div class="form-group">
              <label>Summary</label>
              <textarea id="fSummary" rows="5" placeholder="Write a brief professional summary..." oninput="autoSave();updatePreview()"></textarea>
            </div>
            <button class="btn btn-amber btn-full" onclick="generateSummary()">
              <span data-lucide="sparkles" style="width:18px;height:18px"></span> Generate with AI
            </button>
            <div id="summaryResult" class="mt-16"></div>
            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <button class="btn btn-ghost" onclick="prevStep(1)"><span data-lucide="arrow-left" style="width:18px;height:18px"></span> Back</button>
              <button class="btn btn-primary" onclick="nextStep(3)">Next <span data-lucide="arrow-right" style="width:18px;height:18px"></span></button>
            </div>
          </div>

          <!-- Step 3 - Experience -->
          <div class="step-card" data-step="3">
            <h3 class="step-title">Work Experience</h3>
            <p class="step-subtitle">Add your professional history</p>
            <div id="experienceEntries"></div>
            <button class="btn btn-ghost btn-full" onclick="addExperience()"><span data-lucide="plus" style="width:18px;height:18px"></span> Add Experience</button>
            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <button class="btn btn-ghost" onclick="prevStep(2)"><span data-lucide="arrow-left" style="width:18px;height:18px"></span> Back</button>
              <button class="btn btn-primary" onclick="nextStep(4)">Next <span data-lucide="arrow-right" style="width:18px;height:18px"></span></button>
            </div>
          </div>

          <!-- Step 4 - Education -->
          <div class="step-card" data-step="4">
            <h3 class="step-title">Education</h3>
            <p class="step-subtitle">Your academic background</p>
            <div id="educationEntries"></div>
            <button class="btn btn-ghost btn-full" onclick="addEducation()"><span data-lucide="plus" style="width:18px;height:18px"></span> Add Education</button>
            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <button class="btn btn-ghost" onclick="prevStep(3)"><span data-lucide="arrow-left" style="width:18px;height:18px"></span> Back</button>
              <button class="btn btn-primary" onclick="nextStep(5)">Next <span data-lucide="arrow-right" style="width:18px;height:18px"></span></button>
            </div>
          </div>

          <!-- Step 5 - Skills -->
          <div class="step-card" data-step="5">
            <h3 class="step-title">Skills</h3>
            <p class="step-subtitle">Add your key skills and competencies</p>
            <div class="form-group">
              <label>Your Skills</label>
              <div style="display:flex;gap:8px;margin-bottom:8px">
                <input type="text" id="fSkillInput" placeholder="Type a skill and press Enter" onkeydown="if(event.key==='Enter'){event.preventDefault();addSkill()}">
                <button class="btn btn-primary btn-sm" onclick="addSkill()">Add</button>
              </div>
              <div class="skills-tags" id="skillsTags"></div>
            </div>
            <button class="btn btn-amber btn-full" onclick="suggestSkills()">
              <span data-lucide="sparkles" style="width:18px;height:18px"></span> Suggest Skills with AI
            </button>
            <div id="suggestedSkillsResult" class="mt-16"></div>
            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <button class="btn btn-ghost" onclick="prevStep(4)"><span data-lucide="arrow-left" style="width:18px;height:18px"></span> Back</button>
              <button class="btn btn-primary" onclick="nextStep(6)">Next <span data-lucide="arrow-right" style="width:18px;height:18px"></span></button>
            </div>
          </div>

          <!-- Step 6 - Extras -->
          <div class="step-card" data-step="6">
            <h3 class="step-title">Extras</h3>
            <p class="step-subtitle">Certifications, projects, languages</p>

            <details class="form-group" style="border:1px solid var(--border);border-radius:var(--radius-card);padding:16px;margin-bottom:16px">
              <summary style="cursor:pointer;font-weight:600;font-size:0.9375rem;display:flex;align-items:center;gap:8px">
                <span data-lucide="certificate" style="width:18px;height:18px;color:var(--primary)"></span> Certifications
                <span style="flex:1"></span>
                <span data-lucide="chevron-down" style="width:16px;height:16px"></span>
              </summary>
              <div class="mt-16" id="certContainer">
                <div id="certEntries"></div>
                <button class="btn btn-ghost btn-sm btn-full mt-8" onclick="addCert()"><span data-lucide="plus" style="width:16px;height:16px"></span> Add Certification</button>
              </div>
            </details>

            <details class="form-group" style="border:1px solid var(--border);border-radius:var(--radius-card);padding:16px;margin-bottom:16px">
              <summary style="cursor:pointer;font-weight:600;font-size:0.9375rem;display:flex;align-items:center;gap:8px">
                <span data-lucide="folder-git-2" style="width:18px;height:18px;color:var(--primary)"></span> Projects
                <span style="flex:1"></span>
                <span data-lucide="chevron-down" style="width:16px;height:16px"></span>
              </summary>
              <div class="mt-16" id="projContainer">
                <div id="projectEntries"></div>
                <button class="btn btn-ghost btn-sm btn-full mt-8" onclick="addProject()"><span data-lucide="plus" style="width:16px;height:16px"></span> Add Project</button>
              </div>
            </details>

            <details class="form-group" style="border:1px solid var(--border);border-radius:var(--radius-card);padding:16px">
              <summary style="cursor:pointer;font-weight:600;font-size:0.9375rem;display:flex;align-items:center;gap:8px">
                <span data-lucide="languages" style="width:18px;height:18px;color:var(--primary)"></span> Languages
                <span style="flex:1"></span>
                <span data-lucide="chevron-down" style="width:16px;height:16px"></span>
              </summary>
              <div class="mt-16" id="langContainer">
                <div id="langEntries"></div>
                <button class="btn btn-ghost btn-sm btn-full mt-8" onclick="addLanguage()"><span data-lucide="plus" style="width:16px;height:16px"></span> Add Language</button>
              </div>
            </details>

            <div class="flex gap-16" style="margin-top:24px;justify-content:space-between">
              <button class="btn btn-ghost" onclick="prevStep(5)"><span data-lucide="arrow-left" style="width:18px;height:18px"></span> Back</button>
              <button class="btn btn-primary" onclick="finishBuild()"><span data-lucide="check" style="width:18px;height:18px"></span> Save Resume</button>
            </div>
          </div>

          <!-- ATS Score badge -->
          <div class="mt-24" id="builderScore" style="display:none">
            <div class="glass-card flex items-center justify-between">
              <div>
                <div class="text-sm text-2 font-semibold">ATS Score</div>
                <div class="text-lg font-bold" id="builderScoreText">--</div>
              </div>
              <div class="flex gap-8 items-center">
                <div class="text-xs text-3" id="builderScoreTips"></div>
                <div class="spinner" id="builderScoreSpinner" style="display:none"></div>
              </div>
            </div>
          </div>
        </div>

        <!-- Preview Panel -->
        <div class="builder-preview" id="builderPreview">
          <div class="preview-panel">
            <div class="preview-header">
              <h3><span data-lucide="eye" style="width:16px;height:16px;vertical-align:middle;margin-right:6px"></span> Live Preview</h3>
              <div class="preview-template-select" id="templateSelect"></div>
            </div>
            <div class="preview-body" id="previewBody">
              <div class="preview-watermark">ResumeAI Pro — Preview</div>
              <div id="resumePreview" class="resume-a4 resume-classic">
                <div style="text-align:center;padding:60px 20px;color:#999">
                  <span data-lucide="file-text" style="width:48px;height:48px;margin:0 auto 16px;display:block;opacity:0.3"></span>
                  Your resume will appear here<br><span style="font-size:0.875rem">Fill in the form to get started</span>
                </div>
              </div>
              <div class="flex gap-8 mt-16" style="justify-content:center;flex-wrap:wrap">
                <button class="btn btn-ghost btn-sm" onclick="fullPreview()"><span data-lucide="maximize-2" style="width:16px;height:16px"></span> Full Preview</button>
                <button class="btn btn-primary btn-sm" onclick="downloadResume()"><span data-lucide="download" style="width:16px;height:16px"></span> Download PDF</button>
                <span id="downloadBadge" class="badge badge-premium" style="display:none"></span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Score Page -->
    <div class="page" id="page-score">
      <h2 style="margin-bottom:8px">Score My Resume</h2>
      <p class="text-2" style="margin-bottom:24px">Upload your resume for a free AI-powered ATS analysis</p>

      <div class="score-zone" id="scoreZone" onclick="document.getElementById('resumeFile').click()">
        <div class="upload-icon" data-lucide="cloud-upload" style="width:28px;height:28px"></div>
        <h3 style="font-size:1.125rem;margin-bottom:4px">Drop your resume here</h3>
        <p class="text-3 text-sm">or click to browse — PDF or DOCX</p>
        <input type="file" id="resumeFile" accept=".pdf,.docx" onchange="handleResumeUpload(event)" style="display:none">
      </div>

      <div class="mt-24">
        <details style="border:1px solid var(--border);border-radius:var(--radius-card);padding:16px">
          <summary style="cursor:pointer;font-weight:600;display:flex;align-items:center;gap:8px">
            <span data-lucide="eye" style="width:18px;height:18px;color:var(--primary)"></span> Extracted Text Preview
            <span style="flex:1"></span>
            <span data-lucide="chevron-down" style="width:16px;height:16px"></span>
          </summary>
          <pre id="extractedTextPreview" class="mt-16" style="font-size:0.8125rem;color:var(--text2);white-space:pre-wrap;max-height:300px;overflow-y:auto;background:var(--surface2);padding:12px;border-radius:var(--radius-inp)"></pre>
        </details>
      </div>

      <div class="form-group mt-24">
        <label>Job Description (optional)</label>
        <textarea id="scoreJd" rows="4" placeholder="Paste the job description to get a tailored score..." style="resize:vertical"></textarea>
      </div>

      <button class="btn btn-primary btn-lg btn-full mt-16" onclick="analyzeResume()">
        <span data-lucide="sparkles" style="width:20px;height:20px"></span> Analyze Resume
      </button>

      <div id="scoreResults" style="display:none" class="mt-32"></div>
      <div id="scoreFixSection" style="display:none" class="mt-32"></div>
    </div>

    <!-- Dashboard -->
    <div class="page" id="page-dashboard">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:24px">
        <h2>My Resumes</h2>
        <button class="btn btn-primary btn-sm" onclick="showPage('builder')"><span data-lucide="plus" style="width:16px;height:16px"></span> New</button>
      </div>
      <div id="dashboardContent"></div>
    </div>
  </div>

  <!-- Full Preview Modal -->
  <div class="modal-overlay" id="fullPreviewModal">
    <div class="modal-sheet">
      <div class="grabber"></div>
      <div class="modal-header">
        <h2>Resume Preview</h2>
        <button class="modal-close" onclick="closeModal('fullPreviewModal')" data-lucide="x"></button>
      </div>
      <div class="modal-body" id="fullPreviewBody" style="background:#f5f5f5"></div>
    </div>
  </div>

  <!-- Action Sheet -->
  <div class="modal-overlay" id="actionSheetOverlay" onclick="closeActionSheet()">
    <div class="action-sheet" id="actionSheet" onclick="event.stopPropagation()">
      <div class="as-grabber"></div>
      <div id="actionSheetItems"></div>
    </div>
  </div>

  <!-- Coach marks overlay -->
  <div class="coach-overlay" id="coachOverlay"></div>
</div>

<script>
// ===== CONSTANTS =====
const GEMINI_API_KEY = window._env?.GEMINI_API_KEY || "YOUR_GEMINI_API_KEY";
const RAZORPAY_KEY = window._env?.RAZORPAY_KEY || "YOUR_RAZORPAY_KEY_ID";
const FIREBASE_CONFIG = window._env?.FIREBASE_CONFIG || {
  apiKey: "YOUR_FIREBASE_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID"
};

// ===== STATE =====
let currentPage = 'home';
let currentStep = 1;
let totalSteps = 6;
let selectedTemplate = 'classic';
let isPremium = false;
let skills = [];
let experiences = [];
let educations = [];
let certifications = [];
let projects = [];
let languages = [];
let sessionContext = { accepted: [], rejected: [], tonePreference: 'professional', jobTitle: '', industry: '' };
let acceptedSuggestionCount = 0;
let user = null;
let resumeData = {};
let remainingDownloads = parseInt(localStorage.getItem('resumeai_downloads') || '0');
let isLight = localStorage.getItem('resumeai_theme') === 'light';
let recentResumes = JSON.parse(localStorage.getItem('resumeai_resumes') || '[]');
let uploadedResumeText = '';
let analyzing = false;

// ===== INIT =====
document.addEventListener('DOMContentLoaded', () => {
  if (isLight) document.body.classList.add('light');
  initStepIndicator();
  initTemplateSelect();
  addExperience();
  addEducation();
  renderDashboard();

  // Set pdf.js worker
  if (typeof pdfjsLib !== 'undefined') {
    pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
  }

  // Init download badge
  getRemainingDownloads();

  setTimeout(() => {
    lucide.createIcons();
    checkFirstVisit();
  }, 100);

  // Keyboard save
  document.addEventListener('keydown', (e) => {
    if ((e.ctrlKey || e.metaKey) && e.key === 's') {
      e.preventDefault();
      saveResumeData();
      showToast('Saved ✓');
    }
  });

  // Drag-drop for score zone
  const sz = document.getElementById('scoreZone');
  sz.addEventListener('dragover', (e) => { e.preventDefault(); sz.classList.add('dragover'); });
  sz.addEventListener('dragleave', () => sz.classList.remove('dragover'));
  sz.addEventListener('drop', (e) => { e.preventDefault(); sz.classList.remove('dragover'); if (e.dataTransfer.files.length) handleFile(e.dataTransfer.files[0]); });

  // Init Firebase
  initFirebase();
});

async function initFirebase() {
  if (FIREBASE_CONFIG.apiKey === "YOUR_FIREBASE_API_KEY") return;
  try {
    const { initializeApp } = await import('firebase/app');
    const { getAuth, onAuthStateChanged, signInWithRedirect, getRedirectResult, GoogleAuthProvider, signOut } = await import('firebase/auth');
    const app = initializeApp(FIREBASE_CONFIG);
    const auth = getAuth(app);
    window._auth = auth;
    window._signInWithRedirect = signInWithRedirect;
    window._GoogleAuthProvider = GoogleAuthProvider;
    window._signOut = signOut;

    // Handle redirect result (user coming back from Google)
    try {
      const result = await getRedirectResult(auth);
      if (result) {
        const u = result.user;
        user = { uid: u.uid, name: u.displayName || 'User', email: u.email, photo: u.photoURL };
        updateAuthUI();
        loadUserResumes();
        return;
      }
    } catch (e) {
      console.error('Redirect result error:', e);
    }

    onAuthStateChanged(auth, (u) => {
      if (u) {
        user = { uid: u.uid, name: u.displayName || 'User', email: u.email, photo: u.photoURL };
        updateAuthUI();
        loadUserResumes();
      } else {
        user = null;
        updateAuthUI();
      }
    });
  } catch (e) {
    console.error('Firebase init error:', e);
  }
}

function signIn() {
  if (window._auth && window._signInWithRedirect) {
    window._signInWithRedirect(window._auth, new window._GoogleAuthProvider());
  } else {
    showToast('Firebase not configured. Set FIREBASE_CONFIG.');
  }
}

function signOutUser() {
  if (window._signOut && window._auth) {
    window._signOut(window._auth);
    closeActionSheet();
    showPage('home');
  }
}

function continueWithGoogle() { signIn(); }

function updateAuthUI() {
  const area = document.getElementById('authArea');
  if (user) {
    area.innerHTML = `
      ${user.photo ? `<img class="user-avatar" src="${user.photo}" alt="" onclick="showActionSheet()">` :
        `<div class="user-avatar-placeholder" onclick="showActionSheet()">${user.name[0]}</div>`}
    `;
    document.getElementById('signInBtn') && (document.getElementById('signInBtn').style.display = 'none');
    document.getElementById('landingCta') && (document.getElementById('landingCta').textContent = 'Start Building →');
  } else {
    area.innerHTML = `<button class="btn btn-primary btn-sm" onclick="signIn()" id="signInBtn">Sign In</button>`;
  }
  lucide.createIcons();
}

function showActionSheet() {
  const sheet = document.getElementById('actionSheet');
  const items = document.getElementById('actionSheetItems');
  items.innerHTML = `
    <div class="as-item" onclick="showPage('dashboard');closeActionSheet()">
      <span class="as-icon" data-lucide="file-text" style="width:20px;height:20px"></span> My Resumes
    </div>
    <div class="as-item" onclick="closeActionSheet();toggleTheme()">
      <span class="as-icon" data-lucide="${isLight?'moon':'sun'}" style="width:20px;height:20px"></span> ${isLight?'Dark':'Light'} Mode
    </div>
    <div class="as-item danger" onclick="signOutUser()">
      <span class="as-icon" data-lucide="log-out" style="width:20px;height:20px;color:var(--red)"></span> Sign Out
    </div>
  `;
  document.getElementById('actionSheetOverlay').classList.add('open');
  setTimeout(() => lucide.createIcons(), 50);
}

function closeActionSheet() {
  document.getElementById('actionSheetOverlay').classList.remove('open');
}

// ===== NAVIGATION =====
function showPage(page) {
  if (page === 'builder' && !user) { showToast('Please sign in to use the builder'); return; }
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(`page-${page}`).classList.add('active');
  document.querySelectorAll('.tab-item').forEach(t => t.classList.toggle('active', t.dataset.tab === page));
  currentPage = page;
  if (page === 'dashboard') renderDashboard();
  document.getElementById('mainContent').scrollTo(0,0);
  window.scrollTo(0,0);
}

// ===== TOGGLE THEME =====
function toggleTheme() {
  isLight = !isLight;
  document.body.classList.toggle('light', isLight);
  localStorage.setItem('resumeai_theme', isLight ? 'light' : 'dark');
  const icon = document.getElementById('themeToggle');
  if (icon) { icon.dataset.lucide = isLight ? 'sun' : 'moon'; lucide.createIcons(); }
}

// ===== TOAST =====
let toastTimeout;

function showToast(msg, icon) {
  const t = document.getElementById('toast');
  t.innerHTML = `${icon ? `<span data-lucide="${icon}" style="width:18px;height:18px"></span>` : ''}${msg}`;
  t.classList.add('show');
  clearTimeout(toastTimeout);
  toastTimeout = setTimeout(() => t.classList.remove('show'), 3000);
  setTimeout(() => lucide.createIcons(), 50);
}

// ===== STEP INDICATOR =====
function initStepIndicator() {
  const c = document.getElementById('stepIndicator');
  c.innerHTML = '';
  const labels = ['Personal', 'Summary', 'Experience', 'Education', 'Skills', 'Extras'];
  for (let i = 1; i <= totalSteps; i++) {
    c.innerHTML += `<div class="step-dot" onclick="goToStep(${i})"><div class="dot ${i===1?'active':''}" data-step="${i}"></div><span class="step-label">${labels[i-1]}</span></div>`;
    if (i < totalSteps) c.innerHTML += `<div class="step-connector" data-connector="${i}"></div>`;
  }
  updateStepIndicator();
}

function updateStepIndicator() {
  document.querySelectorAll('.step-dot .dot').forEach(d => {
    const s = parseInt(d.dataset.step);
    d.className = `dot${s < currentStep ? ' done' : ''}${s === currentStep ? ' active' : ''}`;
  });
  document.querySelectorAll('.step-connector').forEach(c => {
    c.className = `step-connector${parseInt(c.dataset.connector) < currentStep ? ' done' : ''}`;
  });
  document.querySelectorAll('.step-card').forEach(c => {
    c.classList.toggle('active', parseInt(c.dataset.step) === currentStep);
  });
}

function goToStep(n) {
  if (n < 1 || n > totalSteps) return;
  currentStep = n;
  updateStepIndicator();
  document.getElementById('mainContent').scrollTo(0,0);
  window.scrollTo(0,0);
}

function nextStep(n) { goToStep(n); }
function prevStep(n) { goToStep(n); }

// ===== RESUME DATA HELPERS =====
function getResumeData() {
  return {
    name: document.getElementById('fName').value,
    title: document.getElementById('fTitle').value,
    email: document.getElementById('fEmail').value,
    phone: document.getElementById('fPhone').value,
    location: document.getElementById('fLocation').value,
    linkedin: document.getElementById('fLinkedin').value,
    portfolio: document.getElementById('fPortfolio').value,
    summary: document.getElementById('fSummary').value,
    experiences: getExperiences(),
    educations: getEducations(),
    skills: skills,
    certifications: getCerts(),
    projects: getProjects(),
    languages: getLangs(),
    template: selectedTemplate
  };
}

function autoSave() {
  clearTimeout(window._autoSaveTimer);
  window._autoSaveTimer = setTimeout(() => { saveResumeData(); }, 500);
}

function saveResumeData() {
  const data = getResumeData();
  resumeData = data;
  if (user) {
    const key = `resumeai_draft_${user.uid}`;
    localStorage.setItem(key, JSON.stringify(data));
  }
  silentReScore();
}

function loadUserResumes() {
  if (user) {
    const saved = localStorage.getItem(`resumeai_draft_${user.uid}`);
    if (saved) { try { const d = JSON.parse(saved); restoreResumeData(d); } catch(e) {} }
  }
}

function restoreResumeData(d) {
  if (!d) return;
  document.getElementById('fName').value = d.name || '';
  document.getElementById('fTitle').value = d.title || '';
  document.getElementById('fEmail').value = d.email || '';
  document.getElementById('fPhone').value = d.phone || '';
  document.getElementById('fLocation').value = d.location || '';
  document.getElementById('fLinkedin').value = d.linkedin || '';
  document.getElementById('fPortfolio').value = d.portfolio || '';
  document.getElementById('fSummary').value = d.summary || '';
  skills = d.skills || [];
  renderSkills();
  if (d.experiences) { experiences = d.experiences; renderExperiences(); }
  if (d.educations) { educations = d.educations; renderEducations(); }
  if (d.certifications) { certifications = d.certifications; renderCerts(); }
  if (d.projects) { projects = d.projects; renderProjects(); }
  if (d.languages) { languages = d.languages; renderLangs(); }
  if (d.template) { selectedTemplate = d.template; updateTemplateSelect(); }
  updatePreview();
}

// ===== EXPERIENCE =====
function addExperience(data) {
  const id = Date.now() + Math.random();
  experiences.push(data || { id, company: '', position: '', startDate: '', endDate: '', description: '' });
  renderExperiences();
}

function removeExperience(id) {
  experiences = experiences.filter(e => e.id !== id);
  renderExperiences();
}

function getExperiences() { return experiences; }

function renderExperiences() {
  const c = document.getElementById('experienceEntries');
  c.innerHTML = experiences.map(e => `
    <div class="entry-card">
      <div class="entry-header">
        <h4><span data-lucide="building-2" style="width:16px;height:16px;vertical-align:middle;margin-right:6px"></span> Experience</h4>
        <button class="entry-remove" onclick="removeExperience(${e.id})" data-lucide="trash-2" style="width:18px;height:18px"></button>
      </div>
      <div class="entry-card-grid">
        <div class="form-group"><label>Company</label><input type="text" value="${e.company}" oninput="updateExpField(${e.id},'company',this.value);updatePreview()" placeholder="Company name"></div>
        <div class="form-group"><label>Position</label><input type="text" value="${e.position}" oninput="updateExpField(${e.id},'position',this.value);updatePreview()" placeholder="Job title"></div>
      </div>
      <div class="entry-card-grid">
        <div class="form-group"><label>Start Date</label><input type="text" value="${e.startDate}" oninput="updateExpField(${e.id},'startDate',this.value);updatePreview()" placeholder="e.g. Jan 2020"></div>
        <div class="form-group"><label>End Date</label><input type="text" value="${e.endDate}" oninput="updateExpField(${e.id},'endDate',this.value);updatePreview()" placeholder="e.g. Present"></div>
      </div>
      <div class="form-group"><label>Description</label><textarea rows="3" oninput="updateExpField(${e.id},'description',this.value);updatePreview()" placeholder="Describe your responsibilities and achievements...">${e.description}</textarea></div>
      <button class="btn btn-amber btn-sm" onclick="enhanceBullets(${e.id},this)"><span data-lucide="sparkles" style="width:14px;height:14px"></span> Improve Bullets</button>
    </div>
  `).join('');
  setTimeout(() => lucide.createIcons(), 50);
}

function updateExpField(id, field, val) {
  const e = experiences.find(x => x.id === id);
  if (e) { e[field] = val; autoSave(); }
}

// ===== EDUCATION =====
function addEducation(data) {
  const id = Date.now() + Math.random() + 1;
  educations.push(data || { id, school: '', degree: '', field: '', gradYear: '' });
  renderEducations();
}

function removeEducation(id) {
  educations = educations.filter(e => e.id !== id);
  renderEducations();
}

function getEducations() { return educations; }

function renderEducations() {
  const c = document.getElementById('educationEntries');
  c.innerHTML = educations.map(e => `
    <div class="entry-card">
      <div class="entry-header">
        <h4><span data-lucide="graduation-cap" style="width:16px;height:16px;vertical-align:middle;margin-right:6px"></span> Education</h4>
        <button class="entry-remove" onclick="removeEducation(${e.id})" data-lucide="trash-2" style="width:18px;height:18px"></button>
      </div>
      <div class="entry-card-grid">
        <div class="form-group"><label>School</label><input type="text" value="${e.school}" oninput="updateEduField(${e.id},'school',this.value);updatePreview()" placeholder="University"></div>
        <div class="form-group"><label>Degree</label><input type="text" value="${e.degree}" oninput="updateEduField(${e.id},'degree',this.value);updatePreview()" placeholder="B.S., M.A."></div>
      </div>
      <div class="entry-card-grid">
        <div class="form-group"><label>Field of Study</label><input type="text" value="${e.field}" oninput="updateEduField(${e.id},'field',this.value);updatePreview()" placeholder="Computer Science"></div>
        <div class="form-group"><label>Graduation Year</label><input type="text" value="${e.gradYear}" oninput="updateEduField(${e.id},'gradYear',this.value);updatePreview()" placeholder="2024"></div>
      </div>
    </div>
  `).join('');
  setTimeout(() => lucide.createIcons(), 50);
}

function updateEduField(id, field, val) {
  const e = educations.find(x => x.id === id);
  if (e) { e[field] = val; autoSave(); }
}

// ===== SKILLS =====
function addSkill() {
  const inp = document.getElementById('fSkillInput');
  const val = inp.value.trim();
  if (val && !skills.includes(val)) {
    skills.push(val);
    renderSkills();
    inp.value = '';
    autoSave();
    updatePreview();
  }
}

function removeSkill(s) {
  skills = skills.filter(x => x !== s);
  renderSkills();
  autoSave();
  updatePreview();
}

function renderSkills() {
  const c = document.getElementById('skillsTags');
  c.innerHTML = skills.map(s => `<span class="skill-tag selected">${s} <span class="remove" onclick="removeSkill('${s}')" data-lucide="x" style="width:12px;height:12px"></span></span>`).join('');
  setTimeout(() => lucide.createIcons(), 50);
}

// ===== EXTRAS =====
function addCert(data) { const id=Date.now()+2; certifications.push(data||{id, name:'', issuer:'', year:''}); renderCerts(); }
function removeCert(id) { certifications=certifications.filter(x=>x.id!==id); renderCerts(); }
function getCerts() { return certifications; }

function renderCerts() {
  const c = document.getElementById('certEntries');
  c.innerHTML = certifications.map(e => `
    <div class="entry-card" style="padding:12px;margin-bottom:8px">
      <div class="flex gap-8" style="flex-wrap:wrap">
        <input type="text" value="${e.name}" oninput="updateCertField(${e.id},'name',this.value)" placeholder="Certification name" style="flex:2;min-width:120px">
        <input type="text" value="${e.issuer}" oninput="updateCertField(${e.id},'issuer',this.value)" placeholder="Issuer" style="flex:1">
        <input type="text" value="${e.year}" oninput="updateCertField(${e.id},'year',this.value)" placeholder="Year" style="width:80px">
        <button class="btn btn-ghost btn-sm" onclick="removeCert(${e.id})" data-lucide="x" style="width:36px;height:36px"></button>
      </div>
    </div>
  `).join('');
  setTimeout(()=>lucide.createIcons(),50);
}
function updateCertField(id,f,v) { const e=certifications.find(x=>x.id===id); if(e){e[f]=v;autoSave();} }

function addProject(data) { const id=Date.now()+3; projects.push(data||{id, name:'', description:''}); renderProjects(); }
function removeProject(id) { projects=projects.filter(x=>x.id!==id); renderProjects(); }
function getProjects() { return projects; }

function renderProjects() {
  const c = document.getElementById('projectEntries');
  c.innerHTML = projects.map(e => `
    <div class="entry-card" style="padding:12px;margin-bottom:8px">
      <div class="flex gap-8 items-center" style="flex-wrap:wrap">
        <input type="text" value="${e.name}" oninput="updateProjField(${e.id},'name',this.value)" placeholder="Project name" style="flex:1;min-width:120px">
        <button class="btn btn-ghost btn-sm" onclick="removeProject(${e.id})" data-lucide="x" style="width:36px;height:36px"></button>
      </div>
      <textarea rows="2" oninput="updateProjField(${e.id},'description',this.value)" placeholder="Brief description..." style="margin-top:8px">${e.description}</textarea>
    </div>
  `).join('');
  setTimeout(()=>lucide.createIcons(),50);
}
function updateProjField(id,f,v) { const e=projects.find(x=>x.id===id); if(e){e[f]=v;autoSave();} }

function addLanguage(data) { const id=Date.now()+4; languages.push(data||{id, language:'', proficiency:''}); renderLangs(); }
function removeLanguage(id) { languages=languages.filter(x=>x.id!==id); renderLangs(); }
function getLangs() { return languages; }

function renderLangs() {
  const c = document.getElementById('langEntries');
  c.innerHTML = languages.map(e => `
    <div class="entry-card" style="padding:12px;margin-bottom:8px">
      <div class="flex gap-8 items-center">
        <input type="text" value="${e.language}" oninput="updateLangField(${e.id},'language',this.value)" placeholder="Language" style="flex:1">
        <select onchange="updateLangField(${e.id},'proficiency',this.value)" style="width:120px">
          <option value="">Level</option>
          <option ${e.proficiency==='Native'?'selected':''}>Native</option>
          <option ${e.proficiency==='Fluent'?'selected':''}>Fluent</option>
          <option ${e.proficiency==='Advanced'?'selected':''}>Advanced</option>
          <option ${e.proficiency==='Intermediate'?'selected':''}>Intermediate</option>
          <option ${e.proficiency==='Basic'?'selected':''}>Basic</option>
        </select>
        <button class="btn btn-ghost btn-sm" onclick="removeLanguage(${e.id})" data-lucide="x" style="width:36px;height:36px"></button>
      </div>
    </div>
  `).join('');
  setTimeout(()=>lucide.createIcons(),50);
}
function updateLangField(id,f,v) { const e=languages.find(x=>x.id===id); if(e){e[f]=v;autoSave();} }

// ===== TEMPLATE SELECT =====
function initTemplateSelect() {
  const c = document.getElementById('templateSelect');
  const templates = [
    {id:'classic', name:'Classic', free:true},
    {id:'modern', name:'Modern', free:false},
    {id:'executive', name:'Executive', free:false},
    {id:'minimal', name:'Minimal', free:false},
    {id:'tech', name:'Tech', free:false}
  ];
  c.innerHTML = templates.map(t => `
    <button class="tmpl-btn ${t.id===selectedTemplate?'active':''}" onclick="selectTemplate('${t.id}')" data-premium="${!t.free}">
      ${t.name}${!t.free ? ' <span class="badge badge-premium" style="font-size:0.625rem;padding:2px 6px;margin-left:4px">Pro</span>' : ''}
    </button>
  `).join('');
}

function selectTemplate(id) {
  selectedTemplate = id;
  updateTemplateSelect();
  updatePreview();
}

function updateTemplateSelect() {
  document.querySelectorAll('.tmpl-btn').forEach(b => {
    b.classList.toggle('active', b.textContent.trim().split(' ')[0].toLowerCase() === selectedTemplate || b.textContent.trim().toLowerCase().startsWith(selectedTemplate));
  });
}

// ===== PREVIEW =====
function updatePreview() {
  clearTimeout(window._previewTimer);
  window._previewTimer = setTimeout(doUpdatePreview, 300);
}

function doUpdatePreview() {
  const data = getResumeData();
  const c = document.getElementById('resumePreview');
  c.className = `resume-a4 resume-${selectedTemplate}`;
  c.innerHTML = renderResumeHTML(data, selectedTemplate);
  // full preview
  const fp = document.getElementById('fullPreviewBody');
  if (fp && document.getElementById('fullPreviewModal').classList.contains('open')) {
    fp.innerHTML = `<div class="resume-a4 resume-${selectedTemplate}" style="box-shadow:none;max-width:595px;margin:0 auto">${renderResumeHTML(data, selectedTemplate)}</div>`;
  }
}

function renderResumeHTML(d, tmpl) {
  if (!d || !d.name) {
    return `<div style="text-align:center;padding:60px 20px;color:#999">
      <span data-lucide="file-text" style="width:48px;height:48px;margin:0 auto 16px;display:block;opacity:0.3"></span>
      Your resume will appear here<br><span style="font-size:0.875rem">Fill in the form to get started</span>
    </div>`;
  }

  const expHtml = (d.experiences||[]).filter(e=>e.company).map(e => `
    <div style="margin-bottom:10px">
      <div class="r-item-title">${e.position || 'Position'}</div>
      <div class="r-item-sub">${e.company}${e.startDate ? ` — ${e.startDate}${e.endDate ? ` – ${e.endDate}` : ''}` : ''}</div>
      ${e.description ? `<div class="r-item-desc">${e.description}</div>` : ''}
    </div>
  `).join('');

  const eduHtml = (d.educations||[]).filter(e=>e.school).map(e => `
    <div style="margin-bottom:8px">
      <div class="r-item-title">${e.degree || ''}${e.field ? ` in ${e.field}` : ''}</div>
      <div class="r-item-sub">${e.school}${e.gradYear ? ` — ${e.gradYear}` : ''}</div>
    </div>
  `).join('');

  const skillHtml = (d.skills||[]).map(s => `<span style="display:inline-block;background:#f0f0f0;padding:2px 10px;border-radius:4px;margin:2px;font-size:0.75rem">${s}</span>`).join(' ');

  const certHtml = (d.certifications||[]).filter(e=>e.name).map(e => `<div style="font-size:0.8125rem;margin-bottom:4px">• ${e.name}${e.issuer ? ` — ${e.issuer}` : ''}${e.year ? ` (${e.year})` : ''}</div>`).join('');
  const projHtml = (d.projects||[]).filter(e=>e.name).map(e => `<div style="margin-bottom:6px"><strong style="font-size:0.8125rem">${e.name}</strong>${e.description ? `<div style="font-size:0.8125rem;color:#555">${e.description}</div>` : ''}</div>`).join('');
  const langHtml = (d.languages||[]).filter(e=>e.language).map(e => `<span style="display:inline-block;margin-right:8px;font-size:0.8125rem">${e.language}${e.proficiency ? ` (${e.proficiency})` : ''}</span>`).join('');

  const contactLine = [d.email, d.phone, d.location].filter(Boolean).join(' · ');
  const linksLine = [d.linkedin, d.portfolio].filter(Boolean).join(' · ');

  switch(tmpl) {
    case 'modern':
      return `<div class="r-sidebar">
        <div class="r-name">${d.name}</div>
        <div class="r-title">${d.title||''}</div>
        <div style="font-size:0.8125rem;margin-bottom:16px;opacity:0.8">${contactLine}</div>
        ${linksLine ? `<div style="font-size:0.75rem;opacity:0.7;margin-bottom:16px">${linksLine}</div>` : ''}
        ${skillHtml ? `<div class="r-section-title">Skills</div><div>${skillHtml}</div>` : ''}
        ${langHtml ? `<div class="r-section-title" style="margin-top:16px">Languages</div><div>${langHtml}</div>` : ''}
      </div>
      <div class="r-main">
        ${d.summary ? `<div class="r-section-title">Summary</div><div class="r-item-desc" style="margin-bottom:16px">${d.summary}</div>` : ''}
        ${expHtml ? `<div class="r-section-title">Experience</div>${expHtml}` : ''}
        ${eduHtml ? `<div class="r-section-title">Education</div>${eduHtml}` : ''}
        ${certHtml ? `<div class="r-section-title">Certifications</div>${certHtml}` : ''}
        ${projHtml ? `<div class="r-section-title">Projects</div>${projHtml}` : ''}
      </div>`;

    case 'executive':
      return `<div class="r-header">
        <div class="r-name">${d.name}</div>
        <div class="r-title">${d.title||''}</div>
        <div class="r-contact">${[contactLine, linksLine].filter(Boolean).join(' · ')}</div>
      </div>
      ${d.summary ? `<div class="r-section-title">Professional Summary</div><div class="r-item-desc" style="margin-bottom:16px">${d.summary}</div>` : ''}
      ${expHtml ? `<div class="r-section-title">Experience</div>${expHtml}` : ''}
      ${eduHtml ? `<div class="r-section-title">Education</div>${eduHtml}` : ''}
      ${skillHtml ? `<div class="r-section-title">Core Skills</div><div style="margin-bottom:16px">${skillHtml}</div>` : ''}
      ${certHtml ? `<div class="r-section-title">Certifications</div>${certHtml}` : ''}
      ${projHtml ? `<div class="r-section-title">Projects</div>${projHtml}` : ''}
      ${langHtml ? `<div class="r-section-title">Languages</div>${langHtml}` : ''}`;

    case 'minimal':
      return `<div class="r-name">${d.name}</div>
      <div class="r-title">${d.title||''}</div>
      <div style="font-size:0.8125rem;color:#888;margin-bottom:16px">${contactLine}${linksLine ? ` · ${linksLine}` : ''}</div>
      <div class="r-divider"></div>
      ${d.summary ? `<div class="r-section-title">About</div><div class="r-item-desc" style="margin-bottom:16px">${d.summary}</div><div class="r-divider"></div>` : ''}
      ${expHtml ? `<div class="r-section-title">Experience</div>${expHtml}<div class="r-divider"></div>` : ''}
      ${eduHtml ? `<div class="r-section-title">Education</div>${eduHtml}<div class="r-divider"></div>` : ''}
      ${skillHtml ? `<div class="r-section-title">Skills</div><div style="margin-bottom:16px">${skillHtml}</div><div class="r-divider"></div>` : ''}
      ${certHtml ? `<div class="r-section-title">Certifications</div>${certHtml}<div class="r-divider"></div>` : ''}
      ${projHtml ? `<div class="r-section-title">Projects</div>${projHtml}<div class="r-divider"></div>` : ''}
      ${langHtml ? `<div class="r-section-title">Languages</div>${langHtml}` : ''}`;

    case 'tech':
      return `<div style="margin-bottom:8px">
        <div class="r-name">${d.name}</div>
        <div class="r-title">${d.title||''}</div>
        <div style="font-size:0.8125rem;color:#666">${contactLine}${linksLine ? ` · ${linksLine}` : ''}</div>
      </div>
      <div class="r-grid">
        ${d.summary ? `<div class="r-section-label">Summary</div><div class="r-section-content"><div class="r-item-desc" style="margin-bottom:8px">${d.summary}</div></div>` : ''}
        ${expHtml ? `<div class="r-section-label">Experience</div><div class="r-section-content">${expHtml}</div>` : ''}
        ${eduHtml ? `<div class="r-section-label">Education</div><div class="r-section-content">${eduHtml}</div>` : ''}
        ${skillHtml ? `<div class="r-section-label">Skills</div><div class="r-section-content"><div style="margin-bottom:8px">${skillHtml}</div></div>` : ''}
        ${certHtml ? `<div class="r-section-label">Certs</div><div class="r-section-content">${certHtml}</div>` : ''}
        ${projHtml ? `<div class="r-section-label">Projects</div><div class="r-section-content">${projHtml}</div>` : ''}
        ${langHtml ? `<div class="r-section-label">Languages</div><div class="r-section-content">${langHtml}</div>` : ''}
      </div>`;

    default: // classic
      return `<div class="r-name">${d.name}</div>
      <div class="r-title">${d.title||''}</div>
      <div style="font-size:0.8125rem;color:#666;margin-bottom:12px">${contactLine}${linksLine ? `<br>${linksLine}` : ''}</div>
      ${d.summary ? `<div class="r-section-title">Summary</div><div class="r-item-desc" style="margin-bottom:12px">${d.summary}</div>` : ''}
      ${expHtml ? `<div class="r-section-title">Experience</div>${expHtml}` : ''}
      ${eduHtml ? `<div class="r-section-title">Education</div>${eduHtml}` : ''}
      ${skillHtml ? `<div class="r-section-title">Skills</div><div style="margin-bottom:12px">${skillHtml}</div>` : ''}
      ${certHtml ? `<div class="r-section-title">Certifications</div>${certHtml}` : ''}
      ${projHtml ? `<div class="r-section-title">Projects</div>${projHtml}` : ''}
      ${langHtml ? `<div class="r-section-title">Languages</div>${langHtml}` : ''}`;
  }
}

function togglePreview() {
  const p = document.getElementById('builderPreview');
  const isHidden = p.style.display === 'none' || getComputedStyle(p).display === 'none';
  if (isHidden) {
    p.style.display = 'block';
    document.getElementById('previewToggle').innerHTML = '<span data-lucide="eye-off" style="width:16px;height:16px"></span> Hide';
  } else {
    p.style.display = 'none';
    document.getElementById('previewToggle').innerHTML = '<span data-lucide="eye" style="width:16px;height:16px"></span> Preview';
  }
  setTimeout(()=>lucide.createIcons(),50);
}

function fullPreview() {
  const data = getResumeData();
  const fp = document.getElementById('fullPreviewBody');
  fp.innerHTML = `<div class="resume-a4 resume-${selectedTemplate}" style="box-shadow:none;max-width:595px;margin:0 auto">${renderResumeHTML(data, selectedTemplate)}</div>`;
  document.getElementById('fullPreviewModal').classList.add('open');
}

function closeModal(id) {
  document.getElementById(id).classList.remove('open');
}

// ===== AI CLOSED LOOP =====
function buildPromptWithContext(userPrompt) {
  const ctx = sessionContext;
  let contextStr = '';
  if (ctx.accepted.length > 0) contextStr += `User previously accepted these suggestions: ${ctx.accepted.join('; ')}. `;
  if (ctx.rejected.length > 0) contextStr += `User previously rejected these suggestions: ${ctx.rejected.join('; ')}. `;
  if (ctx.tonePreference) contextStr += `User's preferred tone: ${ctx.tonePreference}. `;
  if (ctx.jobTitle) contextStr += `User's job title: ${ctx.jobTitle}. `;
  if (ctx.industry) contextStr += `User's industry: ${ctx.industry}. `;
  return contextStr + 'Maintain consistency with their preferred style.\n\n' + userPrompt;
}

async function callGemini(prompt) {
  if (GEMINI_API_KEY === "YOUR_GEMINI_API_KEY") {
    throw new Error('Set your Gemini API key');
  }
  const fullPrompt = buildPromptWithContext(prompt);
  const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ contents: [{ parts: [{ text: fullPrompt }] }] })
  });
  if (!res.ok) throw new Error('API error ' + res.status);
  const data = await res.json();
  return data.candidates[0].content.parts[0].text;
}

function recordAccept(text) {
  sessionContext.accepted.push(text);
  acceptedSuggestionCount++;
  if (acceptedSuggestionCount >= 3) {
    document.getElementById('aiLearnedBadge').style.display = 'inline-flex';
  }
  silentReScore();
}

function recordReject(text) {
  sessionContext.rejected.push(text);
}

// ===== AI FEATURES =====
let _lastSummary = '';

async function generateSummary() {
  const name = document.getElementById('fName').value || 'the candidate';
  const title = document.getElementById('fTitle').value || 'professional';
  const s = document.getElementById('summaryResult');
  s.innerHTML = '<div class="skeleton" style="height:80px"></div>';

  try {
    const prompt = `Write a 3-4 line ATS-optimized professional summary for ${name}, a ${title}. Skills: ${skills.join(', ') || 'various'}. Return ONLY the summary text, no labels.`;
    const text = await callGemini(prompt);
    _lastSummary = text;
    const encoded = encodeURIComponent(text);
    s.innerHTML = `
      <div class="glass-card">
        <div style="font-size:0.9375rem;line-height:1.6">${text}</div>
        <div class="flex gap-8 mt-16">
          <button class="btn btn-primary btn-sm" onclick="acceptSummary(decodeURIComponent('${encoded}'))"><span data-lucide="check" style="width:16px;height:16px"></span> Accept</button>
          <button class="btn btn-ghost btn-sm" onclick="document.getElementById('fSummary').value=decodeURIComponent('${encoded}');showToast('Summary updated');recordAccept('summary generated')"><span data-lucide="edit" style="width:16px;height:16px"></span> Use</button>
          <button class="btn btn-ghost btn-sm" onclick="generateSummary()"><span data-lucide="refresh" style="width:16px;height:16px"></span> Retry</button>
        </div>
      </div>`;
    setTimeout(()=>lucide.createIcons(),50);
  } catch(e) {
    s.innerHTML = `<div class="glass-card" style="color:var(--red)">Error: ${e.message}</div>`;
  }
}

function acceptSummary(text) {
  document.getElementById('fSummary').value = text;
  recordAccept('summary: ' + text.substring(0,50));
  document.getElementById('summaryResult').innerHTML = '<div class="glass-card flex items-center gap-8" style="color:var(--green)"><span data-lucide="check-circle" style="width:18px;height:18px"></span> Summary accepted</div>';
  updatePreview();
  setTimeout(()=>lucide.createIcons(),50);
}

async function enhanceBullets(id, btnEl) {
  const exp = experiences.find(e => e.id === id);
  if (!exp || !exp.description) { showToast('Write a description first'); return; }

  const btn = btnEl || (event && event.target) || document.querySelector(`[onclick*="enhanceBullets(${id}"]`);
  if (btn) { btn.innerHTML = '<span class="spinner" style="width:16px;height:16px"></span> Enhancing...'; btn.disabled = true; }

  try {
    const prompt = `Rewrite these resume bullet points into quantified, action-verb-led lines. Keep the same facts but make them impactful:\n${exp.description}\nReturn ONLY the rewritten text.`;
    const text = await callGemini(prompt);
    exp.description = text;
    renderExperiences();
    recordAccept('enhanced bullets: ' + text.substring(0,50));
    showToast('Bullets enhanced ✓');
    updatePreview();
    silentReScore();
  } catch(e) {
    showToast('Error: ' + e.message);
  }
  if (btn) btn.disabled = false;
}

async function suggestSkills() {
  const title = document.getElementById('fTitle').value;
  if (!title) { showToast('Enter a job title first'); return; }

  const c = document.getElementById('suggestedSkillsResult');
  c.innerHTML = '<div class="skeleton" style="height:60px"></div>';

  try {
    const prompt = `Suggest 10-15 relevant skills for a ${title}. Return ONLY as a comma-separated list, no numbering.`;
    const text = await callGemini(prompt);
    const suggested = text.split(',').map(s => s.trim()).filter(Boolean);
    c.innerHTML = `<div class="glass-card">
      <div class="text-sm font-semibold mb-8">Suggested Skills — click to add</div>
      <div class="skills-tags">${suggested.map(s => `<span class="skill-tag" onclick="addSuggestedSkill(decodeURIComponent('${encodeURIComponent(s)}'))">${s} <span data-lucide="plus" style="width:14px;height:14px"></span></span>`).join('')}</div>
    </div>`;
    setTimeout(()=>lucide.createIcons(),50);
  } catch(e) {
    c.innerHTML = `<div class="glass-card" style="color:var(--red)">Error: ${e.message}</div>`;
  }
}

function addSuggestedSkill(s) {
  if (!skills.includes(s)) { skills.push(s); renderSkills(); autoSave(); updatePreview(); showToast(`Added "${s}"`); }
}

// ===== ATS SCORE (BUILDER) =====
async function silentReScore() {
  if (!user || !document.getElementById('fName').value) return;

  const spinner = document.getElementById('builderScoreSpinner');
  const scoreDiv = document.getElementById('builderScore');
  if (!scoreDiv) return;
  scoreDiv.style.display = 'block';
  spinner.style.display = 'block';

  clearTimeout(window._scoreTimer);
  window._scoreTimer = setTimeout(async () => {
    try {
      const data = getResumeData();
      const prompt = `You are an expert ATS resume analyzer. Score this resume out of 100 and return ONLY valid JSON: {"score":number,"tip":"one specific tip to improve"}. Resume: ${JSON.stringify(data)}`;
      const text = await callGemini(prompt);
      const result = JSON.parse(text.replace(/```json|```/g,'').trim());
      document.getElementById('builderScoreText').textContent = result.score + '/100';
      document.getElementById('builderScoreTips').textContent = result.tip || '';
    } catch(e) {}
    spinner.style.display = 'none';
  }, 2000);
}

// ===== FINISH BUILD =====
function finishBuild() {
  const data = getResumeData();
  if (!data.name) { showToast('Please enter your name'); return; }
  if (!user) { showToast('Please sign in first'); return; }

  const resumes = JSON.parse(localStorage.getItem(`resumeai_resumes_${user.uid}`) || '[]');
  resumes.unshift({ ...data, id: Date.now(), createdAt: new Date().toISOString() });
  localStorage.setItem(`resumeai_resumes_${user.uid}`, JSON.stringify(resumes));
  showToast('Resume saved! 🎉');
  renderDashboard();
}

// ===== DASHBOARD =====
function renderDashboard() {
  const c = document.getElementById('dashboardContent');
  if (!user) {
    c.innerHTML = `<div class="dashboard-empty">
      <div class="de-icon" data-lucide="log-in" style="width:32px;height:32px"></div>
      <h3 style="margin-bottom:8px">Sign in to see your resumes</h3>
      <button class="btn btn-primary" onclick="signIn()">Sign In</button>
    </div>`;
    setTimeout(()=>lucide.createIcons(),50);
    return;
  }

  const resumes = JSON.parse(localStorage.getItem(`resumeai_resumes_${user.uid}`) || '[]');
  if (resumes.length === 0) {
    c.innerHTML = `<div class="dashboard-empty">
      <div class="de-icon" data-lucide="file-plus" style="width:32px;height:32px"></div>
      <h3 style="margin-bottom:8px">No resumes yet</h3>
      <p class="text-2 text-sm" style="margin-bottom:16px">Create your first AI-powered resume</p>
      <button class="btn btn-primary" onclick="showPage('builder')">Create Resume</button>
    </div>`;
  } else {
    c.innerHTML = `<div class="dashboard-grid">${resumes.map(r => {
      const timeAgo = getTimeAgo(r.createdAt);
      const tplNames = {classic:'Classic',modern:'Modern',executive:'Executive',minimal:'Minimal',tech:'Tech'};
      return `<div class="dashboard-item">
        <div class="di-icon" data-lucide="file-text" style="width:22px;height:22px"></div>
        <div class="di-info">
          <h4>${r.name || 'Untitled'}</h4>
          <p>${tplNames[r.template]||'Classic'} · ${r.title || 'No title'} · ${timeAgo}</p>
        </div>
        <div class="di-actions">
          <button class="btn btn-ghost btn-sm" onclick="loadResumeFromDash(${resumes.indexOf(r)})"><span data-lucide="edit" style="width:16px;height:16px"></span></button>
          <button class="btn btn-primary btn-sm" onclick="downloadResumeData(${resumes.indexOf(r)})"><span data-lucide="download" style="width:16px;height:16px"></span></button>
        </div>
      </div>`;
    }).join('')}</div>`;
  }
  setTimeout(()=>lucide.createIcons(),50);
}

function getTimeAgo(dateStr) {
  const diff = Date.now() - new Date(dateStr).getTime();
  const mins = Math.floor(diff / 60000);
  if (mins < 1) return 'just now';
  if (mins < 60) return `${mins}m ago`;
  const hrs = Math.floor(mins / 60);
  if (hrs < 24) return `${hrs}h ago`;
  const days = Math.floor(hrs / 24);
  if (days < 7) return `${days}d ago`;
  return `${Math.floor(days / 7)}w ago`;
}

function loadResumeFromDash(idx) {
  if (!user) return;
  const resumes = JSON.parse(localStorage.getItem(`resumeai_resumes_${user.uid}`) || '[]');
  if (resumes[idx]) {
    restoreResumeData(resumes[idx]);
    showPage('builder');
    showToast('Resume loaded');
  }
}

// ===== RESUME UPLOAD & SCORE =====
function handleResumeUpload(e) {
  const file = e.target.files[0];
  if (file) handleFile(file);
}

async function handleFile(file) {
  const ext = file.name.split('.').pop().toLowerCase();
  if (!['pdf','docx'].includes(ext)) { showToast('Please upload PDF or DOCX'); return; }

  showToast('Extracting text...', 'file-text');
  try {
    let text = '';
    if (ext === 'pdf') {
      const arrayBuffer = await file.arrayBuffer();
      const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;
      for (let i = 1; i <= pdf.numPages; i++) {
        const page = await pdf.getPage(i);
        const content = await page.getTextContent();
        text += content.items.map(item => item.str).join(' ') + '\n';
      }
    } else {
      const arrayBuffer = await file.arrayBuffer();
      const result = await mammoth.extractRawText({ arrayBuffer });
      text = result.value;
    }
    uploadedResumeText = text;
    document.getElementById('extractedTextPreview').textContent = text.substring(0, 2000) + (text.length > 2000 ? '...' : '');
    showToast('Text extracted ✓');
  } catch(e) {
    showToast('Error extracting text: ' + e.message);
  }
}

async function analyzeResume() {
  if (!uploadedResumeText) { showToast('Upload a resume first'); return; }
  if (analyzing) return;
  analyzing = true;

  const btn = document.querySelector('#page-score .btn-primary');
  const originalText = btn.innerHTML;
  btn.innerHTML = '<span class="spinner" style="width:18px;height:18px"></span> Analyzing...';
  btn.disabled = true;

  const resultDiv = document.getElementById('scoreResults');
  resultDiv.style.display = 'block';
  resultDiv.innerHTML = '<div class="skeleton-card"></div><div class="skeleton mt-16" style="height:40px"></div><div class="skeleton mt-8" style="height:40px"></div>';

  try {
    const jd = document.getElementById('scoreJd').value;
    const prompt = `You are an expert ATS resume analyzer and career coach operating in an AI closed loop pipeline.
Analyze the resume and return ONLY valid JSON with this structure:
{
  "ats_score": 0-100,
  "section_scores": {"summary":0,"experience":0,"education":0,"skills":0,"formatting":0},
  "strengths": ["str1","str2","str3"],
  "critical_issues": ["issue1","issue2","issue3"],
  "quick_wins": ["win1","win2","win3"],
  "keywords_present": ["kw1"],
  "keywords_missing": ["kw2"],
  "formatting_warnings": ["fw1"],
  "overall_verdict": "string",
  "improvement_priority": ["action1","action2","action3"]
}
RESUME TEXT: ${uploadedResumeText}
${jd ? `JOB DESCRIPTION: ${jd}` : ''}`;

    const text = await callGemini(prompt);
    const result = JSON.parse(text.replace(/```json|```/g,'').trim());
    renderScoreResults(result);
  } catch(e) {
    resultDiv.innerHTML = `<div class="glass-card" style="color:var(--red);text-align:center;padding:32px">
      <span data-lucide="alert-circle" style="width:32px;height:32px;margin-bottom:12px"></span><br>
      Error: ${e.message}
    </div>`;
    setTimeout(()=>lucide.createIcons(),50);
  }

  btn.innerHTML = originalText;
  btn.disabled = false;
  analyzing = false;
}

function renderScoreResults(r) {
  const c = document.getElementById('scoreResults');
  const score = r.ats_score || 0;
  const ringColor = score >= 70 ? '#10B981' : score >= 40 ? '#F59E0B' : '#EF4444';
  const circumference = 2 * Math.PI * 90;
  const offset = circumference - (score / 100) * circumference;

  c.innerHTML = `
    <div class="card">
      <div class="text-center mb-24">
        <div class="score-ring">
          <svg width="200" height="200" viewBox="0 0 200 200">
            <circle cx="100" cy="100" r="90" fill="none" stroke="var(--surface2)" stroke-width="12"/>
            <circle cx="100" cy="100" r="90" fill="none" stroke="${ringColor}" stroke-width="12"
              stroke-dasharray="${circumference}" stroke-dashoffset="${offset}"
              stroke-linecap="round" transform="rotate(-90,100,100)"
              style="transition: stroke-dashoffset 1s var(--ease-out)"/>
          </svg>
          <div class="score-text">
            <span class="score-num" style="color:${ringColor}">${score}</span>
            <span class="score-label">/100</span>
          </div>
        </div>
      </div>

      <h3 style="margin-bottom:16px">Section Scores</h3>
      ${Object.entries(r.section_scores || {}).map(([k,v]) => {
        const col = v >= 70 ? '#10B981' : v >= 40 ? '#F59E0B' : '#EF4444';
        return `<div class="section-bar">
          <span class="bar-label" style="text-transform:capitalize">${k}</span>
          <div class="bar-track"><div class="bar-fill" style="width:${v}%;background:${col}"></div></div>
          <span class="bar-score">${v}</span>
        </div>`;
      }).join('')}

      <div class="mt-24">
        <h3 style="margin-bottom:12px">Strengths</h3>
        <div class="flex flex-col gap-8">${(r.strengths||[]).map(s => `<div class="glass-card" style="padding:12px 16px;border-left:3px solid var(--green);font-size:0.875rem"><span data-lucide="check-circle" style="width:16px;height:16px;color:var(--green);vertical-align:middle;margin-right:8px"></span>${s}</div>`).join('')}</div>
      </div>

      <div class="mt-16">
        <h3 style="margin-bottom:12px">Critical Issues</h3>
        <div class="flex flex-col gap-8">${(r.critical_issues||[]).map(s => `<div class="glass-card" style="padding:12px 16px;border-left:3px solid var(--red);font-size:0.875rem"><span data-lucide="x-circle" style="width:16px;height:16px;color:var(--red);vertical-align:middle;margin-right:8px"></span>${s}</div>`).join('')}</div>
      </div>

      <div class="mt-16">
        <h3 style="margin-bottom:12px">Quick Wins</h3>
        <div class="flex flex-col gap-8">${(r.quick_wins||[]).map(s => `<div class="glass-card" style="padding:12px 16px;border-left:3px solid var(--amber);font-size:0.875rem"><span data-lucide="zap" style="width:16px;height:16px;color:var(--amber);vertical-align:middle;margin-right:8px"></span>${s}</div>`).join('')}</div>
      </div>

      <div class="mt-16">
        <h3 style="margin-bottom:12px">Keyword Analysis</h3>
        <div class="flex flex-wrap gap-8">
          ${(r.keywords_present||[]).map(k => `<span class="chip chip-green"><span data-lucide="check" style="width:14px;height:14px"></span> ${k}</span>`).join('')}
          ${(r.keywords_missing||[]).map(k => `<span class="chip chip-red"><span data-lucide="x" style="width:14px;height:14px"></span> ${k}</span>`).join('')}
        </div>
      </div>

      ${(r.formatting_warnings||[]).length ? `<div class="mt-16">
        <h3 style="margin-bottom:12px">Formatting</h3>
        <div class="flex flex-col gap-4">${r.formatting_warnings.map(s => `<div style="font-size:0.8125rem;color:var(--text2);display:flex;align-items:center;gap:8px"><span data-lucide="alert-triangle" style="width:14px;height:14px;color:var(--amber)"></span> ${s}</div>`).join('')}</div>
      </div>` : ''}

      <div class="mt-16 glass-card" style="border-left:3px solid var(--primary);padding:16px 20px">
        <div style="font-weight:600;margin-bottom:4px">Overall Verdict</div>
        <div style="font-size:0.9375rem;color:var(--text2)">${r.overall_verdict || ''}</div>
      </div>

      <div class="mt-16">
        <h3 style="margin-bottom:12px">Improvement Priority</h3>
        <ol style="padding-left:20px">${(r.improvement_priority||[]).map((s,i) => `<li style="font-size:0.875rem;margin-bottom:8px;color:var(--text2)"><strong>${i+1}.</strong> ${s}</li>`).join('')}</ol>
      </div>

      <div class="flex gap-16 mt-24" style="flex-wrap:wrap">
        <button class="btn btn-primary" onclick="document.getElementById('scoreFixSection').style.display='block';document.getElementById('scoreFixSection').scrollIntoView({behavior:'smooth'})">
          <span data-lucide="wand-2" style="width:18px;height:18px"></span> Fix These Issues
        </button>
        <button class="btn btn-outline" onclick="downloadScoreReport()">
          <span data-lucide="download" style="width:18px;height:18px"></span> Download Report
        </button>
        <button class="btn btn-ghost" onclick="showPage('builder')">
          <span data-lucide="arrow-right" style="width:18px;height:18px"></span> Build a Better Resume
        </button>
      </div>
    </div>`;

  // Fix section
  const fixDiv = document.getElementById('scoreFixSection');
  fixDiv.innerHTML = `
    <h3 class="mb-16">One-Click AI Fixes</h3>
    <div class="flex flex-col gap-12">
      ${(r.critical_issues||[]).slice(0,3).map((issue, i) => {
        const enc = encodeURIComponent(issue);
        return `<div class="card">
          <div style="font-size:0.875rem;font-weight:600;margin-bottom:8px">${issue}</div>
          <button class="btn btn-amber btn-sm" onclick="fixIssue(${i}, decodeURIComponent('${enc}'))">
            <span data-lucide="sparkles" style="width:16px;height:16px"></span> Fix with AI
          </button>
          <div id="fixResult_${i}" class="mt-8"></div>
        </div>`;
      }).join('')}
    </div>`;

  setTimeout(() => lucide.createIcons(), 50);
}

async function fixIssue(idx, issue) {
  const div = document.getElementById(`fixResult_${idx}`);
  div.innerHTML = '<div class="skeleton" style="height:60px"></div>';
  try {
    const prompt = `The resume has this issue: "${issue}". Rewrite the relevant part of the resume to fix it. Return ONLY the rewritten section. Resume: ${uploadedResumeText.substring(0,2000)}`;
    const text = await callGemini(prompt);
    const enc = encodeURIComponent(issue);
    div.innerHTML = `
      <div class="glass-card">
        <div style="font-size:0.875rem;line-height:1.5">${text}</div>
        <div class="flex gap-8 mt-8">
          <button class="btn btn-primary btn-sm" onclick="acceptFix(${idx})"><span data-lucide="check" style="width:14px;height:14px"></span> Accept</button>
          <button class="btn btn-ghost btn-sm" onclick="fixIssue(${idx}, decodeURIComponent('${enc}'))"><span data-lucide="refresh" style="width:14px;height:14px"></span> Retry</button>
        </div>
      </div>`;
    setTimeout(()=>lucide.createIcons(),50);
  } catch(e) {
    div.innerHTML = `<div style="color:var(--red);font-size:0.875rem">Error: ${e.message}</div>`;
  }
}

function acceptFix(idx) {
  recordAccept('fixed issue #' + idx);
  document.getElementById(`fixResult_${idx}`).innerHTML = '<div class="glass-card flex items-center gap-8" style="color:var(--green)"><span data-lucide="check-circle" style="width:18px;height:18px"></span> Fix accepted ✓</div>';
  setTimeout(()=>lucide.createIcons(),50);
}

async function downloadScoreReport() {
  showToast('Generating report...');
  const el = document.getElementById('scoreResults');
  try {
    const opt = { margin: 10, filename: 'ResumeAI_Score_Report.pdf', html2canvas: { scale: 2 }, jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' } };
    await html2pdf().set(opt).from(el).save();
  } catch(e) {
    showToast('Error generating report');
  }
}

// ===== DOWNLOAD COUNTER =====
function getRemainingDownloads() {
  remainingDownloads = parseInt(localStorage.getItem('resumeai_downloads') || '0');
  const badge = document.getElementById('downloadBadge');
  if (badge) {
    if (remainingDownloads > 0) {
      badge.style.display = 'inline-flex';
      badge.textContent = remainingDownloads + ' download' + (remainingDownloads > 1 ? 's' : '') + ' left';
    } else {
      badge.style.display = 'none';
    }
  }
  return remainingDownloads;
}

function useDownload() {
  let count = parseInt(localStorage.getItem('resumeai_downloads') || '0');
  if (count <= 0) return false;
  count--;
  localStorage.setItem('resumeai_downloads', count.toString());
  getRemainingDownloads();
  return true;
}

function addDownloads(n) {
  let count = parseInt(localStorage.getItem('resumeai_downloads') || '0');
  count += n;
  localStorage.setItem('resumeai_downloads', count.toString());
  getRemainingDownloads();
}

// ===== PDF EXPORT =====
async function downloadResume() {
  const data = getResumeData();
  if (!data.name) { showToast('Fill in your name first'); return; }

  const isPremiumTpl = ['modern','executive','minimal','tech'].includes(selectedTemplate);

  if (isPremiumTpl) {
    if (!useDownload()) {
      showPayModal();
      return;
    }
  }

  showToast('Generating PDF...', 'file-down');
  const el = document.createElement('div');
  el.innerHTML = `<div class="resume-a4 resume-${selectedTemplate}" style="padding:40px 36px;background:#fff;color:#1a1a2e;max-width:595px;font-family:Inter,sans-serif">${renderResumeHTML(data, selectedTemplate)}</div>`;
  el.style.position = 'absolute'; el.style.left = '-9999px'; el.style.top = '0';
  document.body.appendChild(el);

  try {
    const opt = { margin: 0, filename: `${data.name.replace(/ /g,'_')}_Resume.pdf`, html2canvas: { scale: 2, useCORS: true }, jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' } };
    await html2pdf().set(opt).from(el).save();
    const remaining = getRemainingDownloads();
    const msg = remaining > 0 ? `Downloaded ✓ (${remaining} left)` : 'Downloaded ✓';
    showToast(msg);
  } catch(e) {
    showToast('Error generating PDF');
  }
  document.body.removeChild(el);
  getRemainingDownloads();
}

function downloadResumeData(idx) {
  if (!user) return;
  const resumes = JSON.parse(localStorage.getItem(`resumeai_resumes_${user.uid}`) || '[]');
  const r = resumes[idx];
  if (!r) return;

  const isPremiumTpl = ['modern','executive','minimal','tech'].includes(r.template);
  if (isPremiumTpl && getRemainingDownloads() <= 0) {
    showPayModal();
    return;
  }

  restoreResumeData(r);
  setTimeout(() => downloadResume(), 300);
}

// ===== PAYMENT & REDEEM =====
const VALID_REDEEM_CODES = ['TEST10', 'FREE3', 'DEV2024', 'PREMIUM', 'BETA'];

function showPayModal() {
  const overlay = document.createElement('div');
  overlay.className = 'modal-overlay open';
  overlay.id = 'payModal';
  overlay.innerHTML = `
    <div class="modal-sheet">
      <div class="grabber"></div>
      <div class="modal-header">
        <h2>Get 3 Downloads</h2>
        <button class="modal-close" onclick="document.getElementById('payModal').remove()" data-lucide="x"></button>
      </div>
      <div class="modal-body text-center">
        <span data-lucide="download" style="width:48px;height:48px;color:var(--amber);margin-bottom:16px;display:inline-block"></span>
        <h3 style="margin-bottom:8px">Download Any Premium Template</h3>
        <p class="text-2 text-sm" style="margin-bottom:24px">Unlock 3 downloads for any premium template.<br>No time limit. No template restrictions.</p>
        <div style="font-size:2.5rem;font-weight:800;margin-bottom:8px;font-family:var(--font-display)">₹10</div>
        <p class="text-3 text-xs" style="margin-bottom:24px">One-time payment · 3 downloads</p>
        <button class="btn btn-primary btn-lg btn-full" onclick="startPayment()">
          <span data-lucide="lock" style="width:18px;height:18px"></span> Pay ₹10 & Get 3 Downloads
        </button>
        <div class="mt-16" style="border-top:1px solid var(--border);padding-top:16px;margin-top:16px">
          <button class="btn btn-ghost btn-sm" onclick="document.getElementById('redeemSection').classList.toggle('hidden')">
            <span data-lucide="key" style="width:16px;height:16px"></span> Have a redeem code?
          </button>
          <div id="redeemSection" class="hidden mt-8" style="display:none">
            <div class="flex gap-8">
              <input type="text" id="redeemInput" placeholder="Enter code" style="flex:1;text-transform:uppercase" onkeydown="if(event.key==='Enter')redeemCode()">
              <button class="btn btn-primary btn-sm" onclick="redeemCode()">Apply</button>
            </div>
            <p id="redeemMsg" class="text-xs mt-8" style="color:var(--text3)"></p>
          </div>
        </div>
        <p class="text-3 text-xs mt-16">Secure payment via Razorpay</p>
      </div>
    </div>
  `;
  document.body.appendChild(overlay);
  setTimeout(() => lucide.createIcons(), 50);
}

function redeemCode() {
  const input = document.getElementById('redeemInput');
  const msg = document.getElementById('redeemMsg');
  const code = input.value.trim().toUpperCase();
  if (!code) { msg.textContent = 'Enter a code'; msg.style.color = 'var(--red)'; return; }
  if (VALID_REDEEM_CODES.includes(code)) {
    addDownloads(3);
    msg.textContent = '✅ Code accepted! 3 downloads added.';
    msg.style.color = 'var(--green)';
    input.value = '';
    setTimeout(() => document.getElementById('payModal').remove(), 1000);
  } else {
    msg.textContent = '❌ Invalid code';
    msg.style.color = 'var(--red)';
  }
}

function startPayment() {
  if (RAZORPAY_KEY === "YOUR_RAZORPAY_KEY_ID") {
    // Demo mode - add downloads without payment
    addDownloads(3);
    document.getElementById('payModal').remove();
    showToast('Got 3 downloads! (Demo mode)', 'download');
    getRemainingDownloads();
    return;
  }

  const options = {
    key: RAZORPAY_KEY,
    amount: 1000,
    currency: "INR",
    name: "ResumeAI Pro",
    description: "3 Premium Resume Downloads",
    handler: function(response) {
      addDownloads(3);
      document.getElementById('payModal').remove();
      showToast('Payment successful! You have 3 downloads 🎉', 'download');
      getRemainingDownloads();
    },
    prefill: { email: user?.email || '', name: user?.name || '' },
    theme: { color: '#6366F1' }
  };
  const rzp = new Razorpay(options);
  rzp.open();
}

// ===== COACH MARKS =====
function checkFirstVisit() {
  if (localStorage.getItem('resumeai_coach')) return;
  localStorage.setItem('resumeai_coach', '1');
  setTimeout(() => {
    showCoach('Welcome to ResumeAI Pro', 'Build ATS-optimized resumes with AI. Start by signing in, then use the Builder to create your resume.', 'center');
  }, 1000);
}

function showCoach(title, desc, position) {
  const ov = document.getElementById('coachOverlay');
  ov.classList.add('open');
  ov.innerHTML = `<div class="coach-card" style="top:50%;left:50%;transform:translate(-50%,-50%)">
    <div class="coach-title">${title}</div>
    <div class="coach-desc">${desc}</div>
    <div class="coach-actions">
      <button class="btn btn-ghost btn-sm" onclick="document.getElementById('coachOverlay').classList.remove('open')">Skip</button>
      <button class="btn btn-primary btn-sm" onclick="document.getElementById('coachOverlay').classList.remove('open')">Got It</button>
    </div>
  </div>`;
}
</script>
</body>
</html>
